<UserControl x:Class="CollectaMundo.Views.Import.ImportStep03_NameSetMappingView"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:vmsteps="clr-namespace:CollectaMundo.ViewModels.Import.ImportSteps"
                 mc:Ignorable="d"
                 d:DataContext="{d:DesignInstance vmsteps:ImportStep03_NameSetMappingViewModel}"
                 d:Style="{StaticResource {x:Type UserControl}}"
                 d:DesignHeight="600" d:DesignWidth="800">
    <Grid Visibility="{Binding StepContentVisibility}" HorizontalAlignment="Center">
        <Grid.RowDefinitions>
            <!-- Explainer -->
            <RowDefinition Height="Auto"/>
            <!-- Listview -->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <FlowDocumentScrollViewer Grid.Row="0" VerticalAlignment="Top" HorizontalAlignment="Center">
            <FlowDocument>
                <Paragraph>
                    If you are, it means that The Import Wizard was not able to map all cards in import file uniquely to a card in the card database.
                </Paragraph>
                <Paragraph>
                    Fret not, bold mage! Now we will try to map the cards in the import file using the card name + set name and/or set code, assuming they exist in the import file.
                </Paragraph>
                <Paragraph>
                    You must choose a mapping for card name and at least one of either set name or set code. If you can map both, it will try to match using both.
                </Paragraph>
            </FlowDocument>
        </FlowDocumentScrollViewer>
        <ListView Grid.Row="1" ItemsSource="{Binding NameSetMappings}" HorizontalAlignment="Center" VerticalAlignment="Top">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Fields to map" Width="150">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding FieldToMap}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <GridViewColumn Header="Import file header" Width="220">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <ComboBox ItemsSource="{Binding CsvHeaders}"
                                  SelectedItem="{Binding SelectedCsvHeader, Mode=TwoWay}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <GridViewColumn Header="Reset" Width="110">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="   Clear   "
                                Command="{Binding DataContext.ClearSelectedMappingCommand,
                                                  RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>

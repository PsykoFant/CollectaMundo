<UserControl x:Class="CollectaMundo.Views.ContentSection.CardLocations.CardLocationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:utilities="clr-namespace:CollectaMundo.ViewModels.Utilities" 
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=utilities:CardLocationViewModel}"
             d:Style="{StaticResource {x:Type UserControl}}"             
             d:DesignHeight="450"
             d:DesignWidth="800">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="{Binding PageTitle}" Style="{StaticResource BigHeaderLabelStyle}" />
        <Label Grid.Row="1" Content="Where's your cards at?" Style="{StaticResource SmallHeaderLabelStyle}" />
        
        <DataGrid Grid.Row="2" Style="{StaticResource DataGridBaseStyle}" 
                  Width="500" 
                  MinHeight="220"
                  Margin="0,10,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Width="*" Binding="{Binding Name}" />
                <DataGridTextColumn Header="Type" Width="180" Binding="{Binding Type}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>

<UserControl x:Class="CollectaMundo.Views.Shell.TopMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:presentation="clr-namespace:CollectaMundo.Presentation" 
             xmlns:viewmodels="clr-namespace:CollectaMundo.ViewModels" 
             d:DataContext="{d:DesignInstance Type=viewmodels:MainWindowViewModel}"
             mc:Ignorable="d">

    <Grid x:Name="GridTopMenu"
          VerticalAlignment="Top"
          HorizontalAlignment="Center"
          Height="40"
          IsEnabled="{Binding IsTopMenuEnabled}">

        <Button Content="All Cards" Command="{Binding ShowAllCardsPageCommand}" Margin="0,5,510,5">
            <Button.Style>
                <Style TargetType="Button" BasedOn="{StaticResource TopMenuButtonStyle}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsAllCardsPageActive}" Value="True">
                            <Setter Property="Background" Value="#5cb9ca"/>
                            <Setter Property="BorderBrush" Value="#2f6f7a"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>

        <Button Content="My Collection" Command="{Binding ShowMyCollectionPageCommand}" Margin="0,5,170,5">
            <Button.Style>
                <Style TargetType="Button" BasedOn="{StaticResource TopMenuButtonStyle}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsMyCollectionPageActive}" Value="True">
                            <Setter Property="Background" Value="#5cb9ca"/>
                            <Setter Property="BorderBrush" Value="#2f6f7a"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>

        <Button Content="Decks" Margin="170,5,0,5">
            <Button.Style>
                <Style TargetType="Button" BasedOn="{StaticResource TopMenuButtonStyle}">
                    <Style.Triggers>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>

        <Button Content="Utilities" Margin="510,5,0,5">
            <Button.Style>
                <Style TargetType="Button" BasedOn="{StaticResource TopMenuButtonStyle}">
                    <Style.Triggers>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>
    </Grid>
</UserControl>

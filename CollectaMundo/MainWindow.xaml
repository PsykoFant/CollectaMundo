<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"    
    xmlns:core="clr-namespace:Microsoft.Xaml.Behaviors.Core;assembly=Microsoft.Xaml.Behaviors"
    xmlns:views="clr-namespace:CollectaMundo.Views"
    xmlns:System="clr-namespace:System;assembly=System.Runtime" 
    x:Class="CollectaMundo.MainWindow"
    xmlns:sys="clr-namespace:System;assembly=mscorlib" 
    d:DataContext="{d:DesignInstance Type=viewmodels:MainWindowViewModel}"
    mc:Ignorable="d"    
    Title="CollectaMundo"
    Icon="/Resources/Images/Icon.ico" 
    Width="1920"
    Height="960"
    Style="{StaticResource {x:Type Window}}">
    
    <Grid>
        <Grid x:Name="AppWindow" DataContext="{Binding Main}"  Visibility="{Binding MainGridVisibility}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="250"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <!-- Top menu -->
            <views:TopMenuView Grid.Row="0" Grid.ColumnSpan="3" />
            <!-- Side menu -->
            <views:ContentSectionView Grid.Row="1" Grid.Column="1"/>

            <!-- Content section -->
            <views:SideMenuView Grid.Row="1" Grid.Column="0"/>
            
            <!-- Card Images -->
            <views:CardImageView Grid.Row="1" Grid.Column="2"/>
            
        </Grid>
    </Grid>
</Window>

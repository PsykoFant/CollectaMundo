<UserControl x:Class="CollectaMundo.Views.Pages.SharedElements.FilterHeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:pagesVmShared="clr-namespace:CollectaMundo.ViewModels.Pages.SharedElements"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:behaviors="clr-namespace:CollectaMundo.Presentation.Behaviors"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=pagesVmShared:FilterHeaderModel}"
             d:DesignHeight="40"
             d:DesignWidth="300">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Content="{Binding HeaderText}" VerticalAlignment="Center" Grid.Column="0"/>

        <ComboBox Grid.Column="1"
                  Width="{Binding ComboWidth}"
                  ItemsSource="{Binding FilterItem.FilteredOptions}"
                  DisplayMemberPath="OptionName"
                  Text="{Binding FilterItem.FreetextSearch, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  IsTextSearchEnabled="False"
                  IsEditable="True"
                  Height="20"
                  HorizontalAlignment="Stretch">

            <i:Interaction.Triggers>
                <i:EventTrigger EventName="SelectionChanged">
                    <i:InvokeCommandAction Command="{Binding ComboBoxSelectionChangedCommand}"
                                           CommandParameter="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=ComboBox}}"/>
                </i:EventTrigger>
            </i:Interaction.Triggers>

            <i:Interaction.Behaviors>
                <behaviors:ComboBoxUnselector ClearTrigger="{Binding ClearComboBoxSelectionTrigger}" />
            </i:Interaction.Behaviors>

            <ComboBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel />
                </ItemsPanelTemplate>
            </ComboBox.ItemsPanel>
        </ComboBox>
    </Grid>
</UserControl>

<Window
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:CollectaMundo"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:core="clr-namespace:Microsoft.Xaml.Behaviors.Core;assembly=Microsoft.Xaml.Behaviors"
    xmlns:viewmodels="clr-namespace:CollectaMundo.ViewModels"
    xmlns:presentation="clr-namespace:CollectaMundo.Presentation"
    xmlns:converters="clr-namespace:CollectaMundo.Presentation.Converters"
    xmlns:behaviors="clr-namespace:CollectaMundo.Presentation.Behaviors"
    xmlns:validation="clr-namespace:CollectaMundo.Presentation.Validation"
    xmlns:System="clr-namespace:System;assembly=System.Runtime" 
    x:Class="CollectaMundo.MainWindow"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    mc:Ignorable="d"    
    Title="CollectaMundo"
    Icon="/Resources/Images/Icon.ico" 
    Width="1920"
    Height="960"
    PreviewMouseDown="Window_PreviewMouseDown"
    Style="{StaticResource {x:Type Window}}">
    
    <Grid>
        <Grid x:Name="AppWindow" DataContext="{Binding Main}"  Visibility="{Binding MainGridVisibility}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="250"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid x:Name="GridTopMenu" Grid.Row="0" Grid.ColumnSpan="3" VerticalAlignment="Top" HorizontalAlignment="Center" Height="40" IsEnabled="{Binding IsTopMenuEnabled}">
                <Button Command="{Binding ShowSearchAndFilterCommand}"
                        Background="{Binding CurrentPage,Converter={StaticResource PageToBrushConverter},ConverterParameter={x:Static presentation:Page.SearchAndFilter}}"
                        Content="All Cards" 
                         HorizontalAlignment="Center" Margin="0,5,510,0" VerticalAlignment="Top" Width="160" Height="30" Style="{StaticResource TopMenuButtonStyle}">
                    <Button.Effect>
                        <DropShadowEffect ShadowDepth="5" BlurRadius="10" Opacity="0.6"/>
                    </Button.Effect>
                </Button>
                <Button Command="{Binding ShowMyCollectionCommand}"
                        Background="{Binding CurrentPage,Converter={StaticResource PageToBrushConverter},ConverterParameter={x:Static presentation:Page.MyCollection}}"
                        Content="My Collection" 
                         HorizontalAlignment="Center" Margin="0,5,170,0" VerticalAlignment="Top" Width="160" Height="30" Style="{StaticResource TopMenuButtonStyle}">
                    <Button.Effect>
                        <DropShadowEffect ShadowDepth="5" BlurRadius="10" Opacity="0.6"/>
                    </Button.Effect>
                </Button>
                <Button Command="{Binding ShowDecksCommand}"
                        Background="{Binding CurrentPage,Converter={StaticResource PageToBrushConverter},ConverterParameter={x:Static presentation:Page.Decks}}"
                        Content="Decks" 
                         HorizontalAlignment="Center" Margin="170,5,0,0" VerticalAlignment="Top" Width="160" Height="30" Style="{StaticResource TopMenuButtonStyle}">
                    <Button.Effect>
                        <DropShadowEffect ShadowDepth="5" BlurRadius="10" Opacity="0.6"/>
                    </Button.Effect>
                </Button>
                <Button Command="{Binding ShowUtilitiesCommand}"
                        Background="{Binding CurrentPage,Converter={StaticResource PageToBrushConverter},ConverterParameter={x:Static presentation:Page.Utilities}}"
                        Content="Utilities" 
                         HorizontalAlignment="Center" Margin="510,5,0,0" VerticalAlignment="Top" Width="160" Height="30" Style="{StaticResource TopMenuButtonStyle}">
                    <Button.Effect>
                        <DropShadowEffect ShadowDepth="5" BlurRadius="10" Opacity="0.6"/>
                    </Button.Effect>
                </Button>
            </Grid>
            <!-- Side menu -->
            <Grid  x:Name="SideMenu" Grid.Row="1" Grid.Column="0" Width="250" HorizontalAlignment="Left" Margin="0,0,0,0" Visibility="{Binding SideMenuVisibility}">
                <Grid x:Name="GridFiltering" Visibility="{Binding SideMenuFilterVisibility}" d:IsHidden="True">

                    <Button x:Name="ClearFiltersButton" Content="Clear filters" Margin="10,0,0,0" VerticalAlignment="Top" Height="20" Command="{Binding FilterVM.ClearFiltersCommand}"/>

                    <!--Color filtering-->
                    <ListBox ItemsSource="{Binding FilterVM.Filters[Colors].FilterOptions}"
                             SelectionMode="Extended" Margin="0,30,0,0" Width="120" HorizontalAlignment="Left"
                             Height="170" VerticalAlignment="Top" Background="{x:Null}" 
                             BorderBrush="{x:Null}" Foreground="{x:Null}">
                        <ListBox.Resources>
                            <Style TargetType="{x:Type ListBoxItem}">
                                <Setter Property="SnapsToDevicePixels" Value="true" />
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                            <CheckBox Margin="5,2"
                                                      IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                      Content="{Binding OptionName}"
                                                      FontSize="{Binding ., Converter={StaticResource ColorlessFontConverter}, ConverterParameter=30}"/>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.Resources>
                    </ListBox>
                    <ListBox ItemsSource="{Binding ColorIcons.Cards}"
                             Margin="20,30,0,0" Width="50" HorizontalAlignment="Left"
                             Height="160" VerticalAlignment="Top" Background="{x:Null}" 
                             BorderBrush="{x:Null}" Foreground="{x:Null}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Image Source="{Binding ManaCostImage}" Height="15" HorizontalAlignment="Right"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <ComboBox DataContext="{Binding FilterVM.Filters[Colors]}"
                              Width="60"
                              Margin="190,35,0,0"
                              VerticalAlignment="Top"
                              HorizontalAlignment="Right"
                              ItemsSource="{Binding AvailableOperators}"
                              SelectedItem="{Binding OperatorSelection, Mode=TwoWay}">
                    </ComboBox>

                    <!--Mana value filtering-->
                    <Label x:Name="ManaValueLabel" Content="Mana value is: " HorizontalAlignment="Left" Margin="0,190,0,0" VerticalAlignment="Top" Style="{StaticResource BelerenBold}"/>
                    <Grid Margin="10,215,0,0" VerticalAlignment="Top" Height="20" Width="240">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <!-- Operator Selection -->
                        <ComboBox Grid.Column="0"
                                  ItemsSource="{Binding FilterVM.Filters[ManaValue].AvailableOperators}"
                                  SelectedItem="{Binding FilterVM.Filters[ManaValue].OperatorSelection, Mode=TwoWay}">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Converter={StaticResource OperatorToSymbolConverter}}" />
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                        <!-- Numeric Value Selection -->
                        <ComboBox Grid.Column="1"
                                  ItemsSource="{Binding FilterVM.Filters[ManaValue].AvailableNumericOptions}"
                                  SelectedItem="{Binding FilterVM.Filters[ManaValue].SelectedNumericValue, Mode=TwoWay}"
                                  HorizontalAlignment="Right"
                                  Width="80"/>
                    </Grid>

                    <!--Rarity-->
                    <ComboBox DataContext="{Binding FilterVM.Filters[Rarity]}"
                              IsDropDownOpen="{Binding IsDropDownOpen, Mode=TwoWay}"
                              HorizontalAlignment="Left" VerticalAlignment="Top" 
                              Margin="10,245,0,0" 
                              Width="180" Height="20">
                        <ComboBox.Template>
                            <ControlTemplate TargetType="{x:Type ComboBox}">
                                <Grid>
                                    <TextBox 
                                        Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                        Background="Transparent" Padding="3,3,0,0" Margin="0"
                                        Foreground="{Binding TextForeground, Mode=TwoWay}">
                                        <i:Interaction.Triggers>
                                            <i:EventTrigger EventName="GotFocus">
                                                <i:InvokeCommandAction Command="{Binding EmbeddedTextBoxGotFocusCommand}" />
                                            </i:EventTrigger>
                                            <i:EventTrigger EventName="LostFocus">
                                                <i:InvokeCommandAction Command="{Binding EmbeddedTextBoxLostFocusCommand}" />
                                            </i:EventTrigger>
                                        </i:Interaction.Triggers>
                                    </TextBox>
                                    <ToggleButton 
                                        Focusable="false"  ClickMode="Press" HorizontalAlignment="Right"
                                        IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource Mode=TemplatedParent}}">
                                        <TextBlock Text="{Binding ReadableLabel}" Margin="5,0"/>
                                    </ToggleButton>
                                    <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" StaysOpen="False">
                                        <Border Background="White" BorderThickness="1" BorderBrush="Black">
                                            <ListBox ItemsSource="{Binding FilteredOptions}" SelectionMode="Multiple"
                                                     VirtualizingPanel.IsVirtualizing="True"
                                                     ScrollViewer.CanContentScroll="True">
                                                <ListBox.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <VirtualizingStackPanel />
                                                    </ItemsPanelTemplate>
                                                </ListBox.ItemsPanel>
                                                <ListBox.Resources>
                                                    <Style TargetType="{x:Type ListBoxItem}">
                                                        <Setter Property="SnapsToDevicePixels" Value="true"/>
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                                                    <CheckBox Margin="5,2"
                                                                      Width="225"
                                                                      Content="{Binding OptionName}"
                                                                      IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                                      HorizontalContentAlignment="Left">
                                                                        <CheckBox.ContentTemplate>
                                                                            <DataTemplate>
                                                                                <TextBlock Text="{Binding}" 
                                                                                   TextTrimming="CharacterEllipsis"
                                                                                   ToolTip="{Binding}" 
                                                                                   MaxWidth="215"
                                                                                   TextWrapping="NoWrap" />
                                                                            </DataTemplate>
                                                                        </CheckBox.ContentTemplate>
                                                                    </CheckBox>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </ListBox.Resources>
                                            </ListBox>
                                        </Border>
                                    </Popup>
                                </Grid>
                            </ControlTemplate>
                        </ComboBox.Template>
                    </ComboBox>
                    <ComboBox DataContext="{Binding FilterVM.Filters[Rarity]}"
                              Width="60"
                              Margin="190,245,0,0"
                              VerticalAlignment="Top"
                              HorizontalAlignment="Right"
                              ItemsSource="{Binding AvailableOperators}"
                              SelectedItem="{Binding OperatorSelection, Mode=TwoWay}">
                    </ComboBox>

                    <!--SuperTypes-->
                    <ComboBox DataContext="{Binding FilterVM.Filters[SuperTypes]}"
                              IsDropDownOpen="{Binding IsDropDownOpen, Mode=TwoWay}"
                              HorizontalAlignment="Left" VerticalAlignment="Top" 
                              Margin="10,275,0,0" 
                              Width="180" Height="20">
                        <ComboBox.Template>
                            <ControlTemplate TargetType="{x:Type ComboBox}">
                                <Grid>
                                    <TextBox 
                                        Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                        Background="Transparent" Padding="3,3,0,0" Margin="0"
                                        Foreground="{Binding TextForeground, Mode=TwoWay}">
                                        <i:Interaction.Triggers>
                                            <i:EventTrigger EventName="GotFocus">
                                                <i:InvokeCommandAction Command="{Binding EmbeddedTextBoxGotFocusCommand}" />
                                            </i:EventTrigger>
                                            <i:EventTrigger EventName="LostFocus">
                                                <i:InvokeCommandAction Command="{Binding EmbeddedTextBoxLostFocusCommand}" />
                                            </i:EventTrigger>
                                        </i:Interaction.Triggers>
                                    </TextBox>
                                    <ToggleButton 
                                        Focusable="false"  ClickMode="Press" HorizontalAlignment="Right"
                                        IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource Mode=TemplatedParent}}">
                                        <TextBlock Text="{Binding ReadableLabel}" Margin="5,0"/>
                                    </ToggleButton>
                                    <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" StaysOpen="False">
                                        <Border Background="White" BorderThickness="1" BorderBrush="Black">
                                            <ListBox ItemsSource="{Binding FilteredOptions}" SelectionMode="Multiple"
                                                     VirtualizingPanel.IsVirtualizing="True"
                                                     ScrollViewer.CanContentScroll="True">
                                                <ListBox.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <VirtualizingStackPanel />
                                                    </ItemsPanelTemplate>
                                                </ListBox.ItemsPanel>
                                                <ListBox.Resources>
                                                    <Style TargetType="{x:Type ListBoxItem}">
                                                        <Setter Property="SnapsToDevicePixels" Value="true"/>
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                                                    <CheckBox Margin="5,2"
                                                                      Width="225"
                                                                      Content="{Binding OptionName}"
                                                                      IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                                      HorizontalContentAlignment="Left">
                                                                        <CheckBox.ContentTemplate>
                                                                            <DataTemplate>
                                                                                <TextBlock Text="{Binding}" 
                                                                                   TextTrimming="CharacterEllipsis"
                                                                                   ToolTip="{Binding}" 
                                                                                   MaxWidth="215"
                                                                                   TextWrapping="NoWrap" />
                                                                            </DataTemplate>
                                                                        </CheckBox.ContentTemplate>
                                                                    </CheckBox>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </ListBox.Resources>
                                            </ListBox>
                                        </Border>
                                    </Popup>
                                </Grid>
                            </ControlTemplate>
                        </ComboBox.Template>
                    </ComboBox>
                    <ComboBox DataContext="{Binding FilterVM.Filters[SuperTypes]}"
                              Width="60"
                              Margin="190,275,0,0"
                              VerticalAlignment="Top"
                              HorizontalAlignment="Right"
                              ItemsSource="{Binding AvailableOperators}"
                              SelectedItem="{Binding OperatorSelection, Mode=TwoWay}">
                    </ComboBox>

                    <!--Types-->
                    <ComboBox DataContext="{Binding FilterVM.Filters[Types]}"
                              IsDropDownOpen="{Binding IsDropDownOpen, Mode=TwoWay}"
                              HorizontalAlignment="Left" VerticalAlignment="Top" 
                              Margin="10,305,0,0" 
                              Width="180" Height="20">
                        <ComboBox.Template>
                            <ControlTemplate TargetType="{x:Type ComboBox}">
                                <Grid>
                                    <TextBox 
                                        Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                        Background="Transparent" Padding="3,3,0,0" Margin="0"
                                        Foreground="{Binding TextForeground, Mode=TwoWay}">
                                        <i:Interaction.Triggers>
                                            <i:EventTrigger EventName="GotFocus">
                                                <i:InvokeCommandAction Command="{Binding EmbeddedTextBoxGotFocusCommand}" />
                                            </i:EventTrigger>
                                            <i:EventTrigger EventName="LostFocus">
                                                <i:InvokeCommandAction Command="{Binding EmbeddedTextBoxLostFocusCommand}" />
                                            </i:EventTrigger>
                                        </i:Interaction.Triggers>
                                    </TextBox>
                                    <ToggleButton 
                                        Focusable="false"  ClickMode="Press" HorizontalAlignment="Right"
                                        IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource Mode=TemplatedParent}}">
                                        <TextBlock Text="{Binding ReadableLabel}" Margin="5,0"/>
                                    </ToggleButton>
                                    <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" StaysOpen="False">
                                        <Border Background="White" BorderThickness="1" BorderBrush="Black">
                                            <ListBox ItemsSource="{Binding FilteredOptions}" SelectionMode="Multiple"
                                                     VirtualizingPanel.IsVirtualizing="True"
                                                     ScrollViewer.CanContentScroll="True">
                                                <ListBox.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <VirtualizingStackPanel />
                                                    </ItemsPanelTemplate>
                                                </ListBox.ItemsPanel>
                                                <ListBox.Resources>
                                                    <Style TargetType="{x:Type ListBoxItem}">
                                                        <Setter Property="SnapsToDevicePixels" Value="true"/>
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                                                    <CheckBox Margin="5,2"
                                                                      Width="225"
                                                                      Content="{Binding OptionName}"
                                                                      IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                                      HorizontalContentAlignment="Left">
                                                                        <CheckBox.ContentTemplate>
                                                                            <DataTemplate>
                                                                                <TextBlock Text="{Binding}" 
                                                                                   TextTrimming="CharacterEllipsis"
                                                                                   ToolTip="{Binding}" 
                                                                                   MaxWidth="215"
                                                                                   TextWrapping="NoWrap" />
                                                                            </DataTemplate>
                                                                        </CheckBox.ContentTemplate>
                                                                    </CheckBox>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </ListBox.Resources>
                                            </ListBox>
                                        </Border>
                                    </Popup>
                                </Grid>
                            </ControlTemplate>
                        </ComboBox.Template>
                    </ComboBox>
                    <ComboBox DataContext="{Binding FilterVM.Filters[Types]}"
                              Width="60"
                              Margin="190,305,0,0"
                              VerticalAlignment="Top"
                              HorizontalAlignment="Right"
                              ItemsSource="{Binding AvailableOperators}"
                              SelectedItem="{Binding OperatorSelection, Mode=TwoWay}">
                    </ComboBox>

                    <!--SubTypes-->
                    <ComboBox DataContext="{Binding FilterVM.Filters[SubTypes]}"
                              IsDropDownOpen="{Binding IsDropDownOpen, Mode=TwoWay}"
                              HorizontalAlignment="Left" VerticalAlignment="Top" 
                              Margin="10,335,0,0" 
                              Width="180" Height="20">
                        <ComboBox.Template>
                            <ControlTemplate TargetType="{x:Type ComboBox}">
                                <Grid>
                                    <TextBox 
                                        Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                        Background="Transparent" Padding="3,3,0,0" Margin="0"
                                        Foreground="{Binding TextForeground, Mode=TwoWay}">
                                        <i:Interaction.Triggers>
                                            <i:EventTrigger EventName="GotFocus">
                                                <i:InvokeCommandAction Command="{Binding EmbeddedTextBoxGotFocusCommand}" />
                                            </i:EventTrigger>
                                            <i:EventTrigger EventName="LostFocus">
                                                <i:InvokeCommandAction Command="{Binding EmbeddedTextBoxLostFocusCommand}" />
                                            </i:EventTrigger>
                                        </i:Interaction.Triggers>
                                    </TextBox>
                                    <ToggleButton 
                                        Focusable="false"  ClickMode="Press" HorizontalAlignment="Right"
                                        IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource Mode=TemplatedParent}}">
                                        <TextBlock Text="{Binding ReadableLabel}" Margin="5,0"/>
                                    </ToggleButton>
                                    <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" StaysOpen="False">
                                        <Border Background="White" BorderThickness="1" BorderBrush="Black">
                                            <ListBox ItemsSource="{Binding FilteredOptions}" SelectionMode="Multiple"
                                                     VirtualizingPanel.IsVirtualizing="True"
                                                     ScrollViewer.CanContentScroll="True">
                                                <ListBox.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <VirtualizingStackPanel />
                                                    </ItemsPanelTemplate>
                                                </ListBox.ItemsPanel>
                                                <ListBox.Resources>
                                                    <Style TargetType="{x:Type ListBoxItem}">
                                                        <Setter Property="SnapsToDevicePixels" Value="true"/>
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                                                    <CheckBox Margin="5,2"
                                                                      Width="225"
                                                                      Content="{Binding OptionName}"
                                                                      IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                                      HorizontalContentAlignment="Left">
                                                                        <CheckBox.ContentTemplate>
                                                                            <DataTemplate>
                                                                                <TextBlock Text="{Binding}" 
                                                                                   TextTrimming="CharacterEllipsis"
                                                                                   ToolTip="{Binding}" 
                                                                                   MaxWidth="215"
                                                                                   TextWrapping="NoWrap" />
                                                                            </DataTemplate>
                                                                        </CheckBox.ContentTemplate>
                                                                    </CheckBox>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </ListBox.Resources>
                                            </ListBox>
                                        </Border>
                                    </Popup>
                                </Grid>
                            </ControlTemplate>
                        </ComboBox.Template>
                    </ComboBox>
                    <ComboBox DataContext="{Binding FilterVM.Filters[SubTypes]}"
                              Width="60"
                              Margin="190,335,0,0"
                              VerticalAlignment="Top"
                              HorizontalAlignment="Right"
                              ItemsSource="{Binding AvailableOperators}"
                              SelectedItem="{Binding OperatorSelection, Mode=TwoWay}">
                    </ComboBox>

                    <!--Keywords-->
                    <ComboBox DataContext="{Binding FilterVM.Filters[Keywords]}"
                              IsDropDownOpen="{Binding IsDropDownOpen, Mode=TwoWay}"
                              HorizontalAlignment="Left" VerticalAlignment="Top" 
                              Margin="10,365,0,0" 
                              Width="180" Height="20">
                        <ComboBox.Template>
                            <ControlTemplate TargetType="{x:Type ComboBox}">
                                <Grid>
                                    <TextBox 
                                        Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                        Background="Transparent" Padding="3,3,0,0" Margin="0"
                                        Foreground="{Binding TextForeground, Mode=TwoWay}">
                                        <i:Interaction.Triggers>
                                            <i:EventTrigger EventName="GotFocus">
                                                <i:InvokeCommandAction Command="{Binding EmbeddedTextBoxGotFocusCommand}" />
                                            </i:EventTrigger>
                                            <i:EventTrigger EventName="LostFocus">
                                                <i:InvokeCommandAction Command="{Binding EmbeddedTextBoxLostFocusCommand}" />
                                            </i:EventTrigger>
                                        </i:Interaction.Triggers>
                                    </TextBox>
                                    <ToggleButton 
                                        Focusable="false"  ClickMode="Press" HorizontalAlignment="Right"
                                        IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource Mode=TemplatedParent}}">                                    
                                        <TextBlock Text="{Binding ReadableLabel}" Margin="5,0"/>
                                    </ToggleButton>
                                    <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" StaysOpen="False">
                                        <Border Background="White" BorderThickness="1" BorderBrush="Black">
                                            <ListBox ItemsSource="{Binding FilteredOptions}" SelectionMode="Multiple"
                                                     VirtualizingPanel.IsVirtualizing="True"
                                                     ScrollViewer.CanContentScroll="True">
                                                <ListBox.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <VirtualizingStackPanel />
                                                    </ItemsPanelTemplate>
                                                </ListBox.ItemsPanel>
                                                <ListBox.Resources>
                                                    <Style TargetType="{x:Type ListBoxItem}">
                                                        <Setter Property="SnapsToDevicePixels" Value="true"/>
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                                                    <CheckBox Margin="5,2"
                                                                      Width="225"
                                                                      Content="{Binding OptionName}"
                                                                      IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                                      HorizontalContentAlignment="Left">
                                                                        <CheckBox.ContentTemplate>
                                                                            <DataTemplate>
                                                                                <TextBlock Text="{Binding}" 
                                                                                   TextTrimming="CharacterEllipsis"
                                                                                   ToolTip="{Binding}" 
                                                                                   MaxWidth="215"
                                                                                   TextWrapping="NoWrap" />
                                                                            </DataTemplate>
                                                                        </CheckBox.ContentTemplate>
                                                                    </CheckBox>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </ListBox.Resources>
                                            </ListBox>
                                        </Border>
                                    </Popup>
                                </Grid>
                            </ControlTemplate>
                        </ComboBox.Template>
                    </ComboBox>
                    <ComboBox DataContext="{Binding FilterVM.Filters[Keywords]}"
                              Width="60"
                              Margin="190,365,0,0"
                              VerticalAlignment="Top"
                              HorizontalAlignment="Right"
                              ItemsSource="{Binding AvailableOperators}"
                              SelectedItem="{Binding OperatorSelection, Mode=TwoWay}">
                    </ComboBox>

                    <!--Finishes-->
                    <ComboBox DataContext="{Binding FilterVM.Filters[Finishes]}"
                              IsDropDownOpen="{Binding IsDropDownOpen, Mode=TwoWay}"
                              HorizontalAlignment="Left" VerticalAlignment="Top" 
                              Margin="10,395,0,0" 
                              Width="180" Height="20">
                        <ComboBox.Template>
                            <ControlTemplate TargetType="{x:Type ComboBox}">
                                <Grid>
                                    <TextBox 
                                        Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                        Background="Transparent" Padding="3,3,0,0" Margin="0"
                                        Foreground="{Binding TextForeground, Mode=TwoWay}">
                                        <i:Interaction.Triggers>
                                            <i:EventTrigger EventName="GotFocus">
                                                <i:InvokeCommandAction Command="{Binding EmbeddedTextBoxGotFocusCommand}" />
                                            </i:EventTrigger>
                                            <i:EventTrigger EventName="LostFocus">
                                                <i:InvokeCommandAction Command="{Binding EmbeddedTextBoxLostFocusCommand}" />
                                            </i:EventTrigger>
                                        </i:Interaction.Triggers>
                                    </TextBox>
                                    <ToggleButton 
                                        Focusable="false"  ClickMode="Press" HorizontalAlignment="Right"
                                        IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource Mode=TemplatedParent}}">
                                        <TextBlock Text="{Binding ReadableLabel}" Margin="5,0"/>
                                    </ToggleButton>
                                    <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" StaysOpen="False">
                                        <Border Background="White" BorderThickness="1" BorderBrush="Black">
                                            <ListBox ItemsSource="{Binding FilteredOptions}" SelectionMode="Multiple"
                                                     VirtualizingPanel.IsVirtualizing="True"
                                                     ScrollViewer.CanContentScroll="True">
                                                <ListBox.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <VirtualizingStackPanel />
                                                    </ItemsPanelTemplate>
                                                </ListBox.ItemsPanel>
                                                <ListBox.Resources>
                                                    <Style TargetType="{x:Type ListBoxItem}">
                                                        <Setter Property="SnapsToDevicePixels" Value="true"/>
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                                                    <CheckBox Margin="5,2"
                                                                      Width="225"
                                                                      Content="{Binding OptionName}"
                                                                      IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                                      HorizontalContentAlignment="Left">
                                                                        <CheckBox.ContentTemplate>
                                                                            <DataTemplate>
                                                                                <TextBlock Text="{Binding}" 
                                                                                   TextTrimming="CharacterEllipsis"
                                                                                   ToolTip="{Binding}" 
                                                                                   MaxWidth="215"
                                                                                   TextWrapping="NoWrap" />
                                                                            </DataTemplate>
                                                                        </CheckBox.ContentTemplate>
                                                                    </CheckBox>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </ListBox.Resources>
                                            </ListBox>
                                        </Border>
                                    </Popup>
                                </Grid>
                            </ControlTemplate>
                        </ComboBox.Template>
                    </ComboBox>
                    <ComboBox DataContext="{Binding FilterVM.Filters[Finishes]}"
                              Width="60"
                              Margin="190,395,0,0"
                              VerticalAlignment="Top"
                              HorizontalAlignment="Right"
                              ItemsSource="{Binding AvailableOperators}"
                              SelectedItem="{Binding OperatorSelection, Mode=TwoWay}">
                    </ComboBox>

                    <!--Rulestext filtering-->
                    <TextBox x:Name="FilterTextTextBox"
                             DataContext="{Binding FilterVM.Filters[Text]}"
                             Text="{Binding FreetextSearch, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                             HorizontalAlignment="Left" 
                             Margin="10,425,0,0" 
                             TextWrapping="Wrap" 
                             Foreground="{Binding TextForeground, Mode=TwoWay}" 
                             VerticalAlignment="Top" 
                             Width="240" Height="45" 
                             Background="White"
                             PreviewKeyDown="FilterTextTextBox_KeyDown">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="GotFocus">
                                    <i:InvokeCommandAction Command="{Binding RulesTextBoxGotFocusCommand}" />
                                </i:EventTrigger>
                                <i:EventTrigger EventName="LostFocus">
                                    <i:InvokeCommandAction Command="{Binding RulesTextBoxLostFocusCommand}" />
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                    </TextBox>                

                    <!--Language-->
                    <ComboBox DataContext="{Binding FilterVM.Filters[Language]}"
                              IsDropDownOpen="{Binding IsDropDownOpen, Mode=TwoWay}"
                              HorizontalAlignment="Left" VerticalAlignment="Top" 
                              Margin="10,515,0,0" 
                              Width="180" Height="20"
                              Visibility="{Binding DataContext.Main.CurrentPage,RelativeSource={RelativeSource AncestorType=Window},Converter={StaticResource PageToVisibilityConverter},ConverterParameter={x:Static presentation:Page.MyCollection}}">
                        <ComboBox.Template>
                            <ControlTemplate TargetType="{x:Type ComboBox}">
                                <Grid>
                                    <TextBox 
                                        Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                        Background="Transparent" Padding="3,3,0,0" Margin="0"
                                        Foreground="{Binding TextForeground, Mode=TwoWay}">
                                        <i:Interaction.Triggers>
                                            <i:EventTrigger EventName="GotFocus">
                                                <i:InvokeCommandAction Command="{Binding EmbeddedTextBoxGotFocusCommand}" />
                                            </i:EventTrigger>
                                            <i:EventTrigger EventName="LostFocus">
                                                <i:InvokeCommandAction Command="{Binding EmbeddedTextBoxLostFocusCommand}" />
                                            </i:EventTrigger>
                                        </i:Interaction.Triggers>
                                    </TextBox>
                                    <ToggleButton 
                                        Focusable="false"  ClickMode="Press" HorizontalAlignment="Right"
                                        IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource Mode=TemplatedParent}}">
                                        <TextBlock Text="{Binding ReadableLabel}" Margin="5,0"/>
                                    </ToggleButton>
                                    <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" StaysOpen="False">
                                        <Border Background="White" BorderThickness="1" BorderBrush="Black">
                                            <ListBox ItemsSource="{Binding FilteredOptions}" SelectionMode="Multiple"
                                                     VirtualizingPanel.IsVirtualizing="True"
                                                     ScrollViewer.CanContentScroll="True">
                                                <ListBox.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <VirtualizingStackPanel />
                                                    </ItemsPanelTemplate>
                                                </ListBox.ItemsPanel>
                                                <ListBox.Resources>
                                                    <Style TargetType="{x:Type ListBoxItem}">
                                                        <Setter Property="SnapsToDevicePixels" Value="true"/>
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                                                    <CheckBox Margin="5,2"
                                                                      Width="225"
                                                                      Content="{Binding OptionName}"
                                                                      IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                                      HorizontalContentAlignment="Left">
                                                                        <CheckBox.ContentTemplate>
                                                                            <DataTemplate>
                                                                                <TextBlock Text="{Binding}" 
                                                                                   TextTrimming="CharacterEllipsis"
                                                                                   ToolTip="{Binding}" 
                                                                                   MaxWidth="215"
                                                                                   TextWrapping="NoWrap" />
                                                                            </DataTemplate>
                                                                        </CheckBox.ContentTemplate>
                                                                    </CheckBox>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </ListBox.Resources>
                                            </ListBox>
                                        </Border>
                                    </Popup>
                                </Grid>
                            </ControlTemplate>
                        </ComboBox.Template>
                    </ComboBox>
                    <ComboBox DataContext="{Binding FilterVM.Filters[Language]}"
                              Width="60"
                              Margin="190,515,0,0"
                              VerticalAlignment="Top"
                              HorizontalAlignment="Right"
                              ItemsSource="{Binding AvailableOperators}"
                              SelectedItem="{Binding OperatorSelection, Mode=TwoWay}"
                              Visibility="{Binding DataContext.Main.CurrentPage,RelativeSource={RelativeSource AncestorType=Window},Converter={StaticResource PageToVisibilityConverter},ConverterParameter={x:Static presentation:Page.MyCollection}}">
                    </ComboBox>

                    <!--SelectedCondition-->
                    <ComboBox DataContext="{Binding FilterVM.Filters[SelectedCondition]}"
                              IsDropDownOpen="{Binding IsDropDownOpen, Mode=TwoWay}"
                              HorizontalAlignment="Left" VerticalAlignment="Top" 
                              Margin="10,545,0,0" 
                              Width="180" Height="20"
                              Visibility="{Binding DataContext.Main.CurrentPage,RelativeSource={RelativeSource AncestorType=Window},Converter={StaticResource PageToVisibilityConverter},ConverterParameter={x:Static presentation:Page.MyCollection}}">
                        <ComboBox.Template>
                            <ControlTemplate TargetType="{x:Type ComboBox}">
                                <Grid>
                                    <TextBox 
                                        Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                        Background="Transparent" Padding="3,3,0,0" Margin="0"
                                        Foreground="{Binding TextForeground, Mode=TwoWay}">
                                        <i:Interaction.Triggers>
                                            <i:EventTrigger EventName="GotFocus">
                                                <i:InvokeCommandAction Command="{Binding EmbeddedTextBoxGotFocusCommand}" />
                                            </i:EventTrigger>
                                            <i:EventTrigger EventName="LostFocus">
                                                <i:InvokeCommandAction Command="{Binding EmbeddedTextBoxLostFocusCommand}" />
                                            </i:EventTrigger>
                                        </i:Interaction.Triggers>
                                    </TextBox>
                                    <ToggleButton 
                                        Focusable="false"  ClickMode="Press" HorizontalAlignment="Right"
                                        IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource Mode=TemplatedParent}}">
                                        <TextBlock Text="{Binding ReadableLabel}" Margin="5,0"/>
                                    </ToggleButton>
                                    <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" StaysOpen="False">
                                        <Border Background="White" BorderThickness="1" BorderBrush="Black">
                                            <ListBox ItemsSource="{Binding FilteredOptions}" SelectionMode="Multiple"
                                                     VirtualizingPanel.IsVirtualizing="True"
                                                     ScrollViewer.CanContentScroll="True">
                                                <ListBox.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <VirtualizingStackPanel />
                                                    </ItemsPanelTemplate>
                                                </ListBox.ItemsPanel>
                                                <ListBox.Resources>
                                                    <Style TargetType="{x:Type ListBoxItem}">
                                                        <Setter Property="SnapsToDevicePixels" Value="true"/>
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                                                    <CheckBox Margin="5,2"
                                                                      Width="225"
                                                                      Content="{Binding OptionName}"
                                                                      IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                                      HorizontalContentAlignment="Left">
                                                                        <CheckBox.ContentTemplate>
                                                                            <DataTemplate>
                                                                                <TextBlock Text="{Binding}" 
                                                                                   TextTrimming="CharacterEllipsis"
                                                                                   ToolTip="{Binding}" 
                                                                                   MaxWidth="215"
                                                                                   TextWrapping="NoWrap" />
                                                                            </DataTemplate>
                                                                        </CheckBox.ContentTemplate>
                                                                    </CheckBox>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </ListBox.Resources>
                                            </ListBox>
                                        </Border>
                                    </Popup>
                                </Grid>
                            </ControlTemplate>
                        </ComboBox.Template>
                    </ComboBox>
                    <ComboBox DataContext="{Binding FilterVM.Filters[SelectedCondition]}"
                              Width="60"
                              Margin="190,545,0,0"
                              VerticalAlignment="Top"
                              HorizontalAlignment="Right"
                              ItemsSource="{Binding AvailableOperators}"
                              SelectedItem="{Binding OperatorSelection, Mode=TwoWay}"
                              Visibility="{Binding DataContext.Main.CurrentPage,RelativeSource={RelativeSource AncestorType=Window},Converter={StaticResource PageToVisibilityConverter},ConverterParameter={x:Static presentation:Page.MyCollection}}">
                    </ComboBox>

                    <!--SelectedFinish-->
                    <ComboBox DataContext="{Binding FilterVM.Filters[SelectedFinish]}"
                              IsDropDownOpen="{Binding IsDropDownOpen, Mode=TwoWay}"
                              HorizontalAlignment="Left" VerticalAlignment="Top" 
                              Margin="10,485,0,0" 
                              Width="180" Height="20"
                              Visibility="{Binding DataContext.Main.CurrentPage,RelativeSource={RelativeSource AncestorType=Window},Converter={StaticResource PageToVisibilityConverter},ConverterParameter={x:Static presentation:Page.MyCollection}}">
                        <ComboBox.Template>
                            <ControlTemplate TargetType="{x:Type ComboBox}">
                                <Grid>
                                    <TextBox 
                                        Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                        Background="Transparent" Padding="3,3,0,0" Margin="0"
                                        Foreground="{Binding TextForeground, Mode=TwoWay}">
                                        <i:Interaction.Triggers>
                                            <i:EventTrigger EventName="GotFocus">
                                                <i:InvokeCommandAction Command="{Binding EmbeddedTextBoxGotFocusCommand}" />
                                            </i:EventTrigger>
                                            <i:EventTrigger EventName="LostFocus">
                                                <i:InvokeCommandAction Command="{Binding EmbeddedTextBoxLostFocusCommand}" />
                                            </i:EventTrigger>
                                        </i:Interaction.Triggers>
                                    </TextBox>
                                    <ToggleButton 
                                        Focusable="false"  ClickMode="Press" HorizontalAlignment="Right"
                                        IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource Mode=TemplatedParent}}">
                                        <TextBlock Text="{Binding ReadableLabel}" Margin="5,0"/>
                                    </ToggleButton>
                                    <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" StaysOpen="False">
                                        <Border Background="White" BorderThickness="1" BorderBrush="Black">
                                            <ListBox ItemsSource="{Binding FilteredOptions}" SelectionMode="Multiple"
                                                     VirtualizingPanel.IsVirtualizing="True"
                                                     ScrollViewer.CanContentScroll="True">
                                                <ListBox.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <VirtualizingStackPanel />
                                                    </ItemsPanelTemplate>
                                                </ListBox.ItemsPanel>
                                                <ListBox.Resources>
                                                    <Style TargetType="{x:Type ListBoxItem}">
                                                        <Setter Property="SnapsToDevicePixels" Value="true"/>
                                                        <Setter Property="Template">
                                                            <Setter.Value>
                                                                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                                                    <CheckBox Margin="5,2"
                                                                      Width="225"
                                                                      Content="{Binding OptionName}"
                                                                      IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                                                      HorizontalContentAlignment="Left">
                                                                        <CheckBox.ContentTemplate>
                                                                            <DataTemplate>
                                                                                <TextBlock Text="{Binding}" 
                                                                                   TextTrimming="CharacterEllipsis"
                                                                                   ToolTip="{Binding}" 
                                                                                   MaxWidth="215"
                                                                                   TextWrapping="NoWrap" />
                                                                            </DataTemplate>
                                                                        </CheckBox.ContentTemplate>
                                                                    </CheckBox>
                                                                </ControlTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </Style>
                                                </ListBox.Resources>
                                            </ListBox>
                                        </Border>
                                    </Popup>
                                </Grid>
                            </ControlTemplate>
                        </ComboBox.Template>
                    </ComboBox>
                    <ComboBox DataContext="{Binding FilterVM.Filters[SelectedFinish]}"
                              Width="60"
                              Margin="190,485,0,0"
                              VerticalAlignment="Top"
                              HorizontalAlignment="Right"
                              ItemsSource="{Binding AvailableOperators}"
                              SelectedItem="{Binding OperatorSelection, Mode=TwoWay}"
                              Visibility="{Binding DataContext.Main.CurrentPage,RelativeSource={RelativeSource AncestorType=Window},Converter={StaticResource PageToVisibilityConverter},ConverterParameter={x:Static presentation:Page.MyCollection}}">
                    </ComboBox>

                    <CheckBox  Content="Filter on cards for trade"
                               IsChecked="{Binding FilterVM.Filters[CardsForTrade].IsTradeChecked, Mode=TwoWay}"
                               HorizontalAlignment="Left" Margin="10,570,0,0" VerticalAlignment="Top"
                               Visibility="{Binding DataContext.Main.CurrentPage,RelativeSource={RelativeSource AncestorType=Window},Converter={StaticResource PageToVisibilityConverter},ConverterParameter={x:Static presentation:Page.MyCollection}}" />
                    <CheckBox Content="Filter on cards NOT for trade"
                              IsChecked="{Binding FilterVM.Filters[CardsForTrade].IsNotTradeChecked, Mode=TwoWay}"
                              HorizontalAlignment="Left" Margin="10,590,0,0" VerticalAlignment="Top"
                              Visibility="{Binding DataContext.Main.CurrentPage,RelativeSource={RelativeSource AncestorType=Window},Converter={StaticResource PageToVisibilityConverter},ConverterParameter={x:Static presentation:Page.MyCollection}}"/>

                </Grid>
                <Grid x:Name="GridUtilsMenu" Visibility="{Binding SideMenuUtilsVisibility}">
                    <Button Command="{Binding UpdateVM.BackupCollectionCommand}" Content="Create backup of my collection" HorizontalAlignment="Left" Margin="10,0,0,0" Height="20" Width="180" VerticalAlignment="Top"/>
                    <Button x:Name="ImportCollectionButton" Content="The Import Wizard" HorizontalAlignment="Left" Margin="10,30,0,0" Width="180" Height="20" VerticalAlignment="Top" Click="ImportCollectionButton_Click"/>

                    <Label Content="{Binding PricesVM.LatestPriceUpdateDate}" Margin="10,70,0,0" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                    <Button Command="{Binding UpdateVM.UpdatePricesCommand}" Content="Update card prices" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,100,0,0" Width="180" Height="20"/>                    
                    <Label Content="Show prices from:" Margin="10,125,0,0" VerticalAlignment="Top" HorizontalAlignment="Left"/>
                    <ComboBox ItemsSource="{Binding PricesVM.Retailers}"
                              SelectedItem="{Binding PricesVM.SelectedRetailer, Mode=TwoWay}"
                              DisplayMemberPath="Display" SelectedValuePath="Key" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,155,0,0">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="SelectionChanged">
                                <i:InvokeCommandAction
                                    Command="{Binding PricesVM.ChangeRetailerCommand}"
                                    CommandParameter="{Binding SelectedValue, RelativeSource={RelativeSource AncestorType=ComboBox}}" />
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                    </ComboBox>
                    <Button Command="{Binding UpdateVM.CheckForDbUpdatesCommand}"
                            Content="Check for card database updates"                            
                            HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,220,0,0" Width="180" Height="20"/>
                    <Button Command="{Binding UpdateVM.UpdateDBCommand}"
                            Content="Update card database"
                            Visibility="{Binding UpdateVM.UpdateDbVisibility}"
                            HorizontalAlignment="Left" Width="180" Height="20" Margin="10,250,0,0" VerticalAlignment="Top"/>
                </Grid>
            </Grid>
            <!-- Content section -->
            <Grid Grid.Row="1" Grid.Column="1" Margin="5,0,5,0" Visibility="{Binding ContentSectionVisibility}">
                <Image Source="/Resources/Images/Logo.png"
                       Stretch="Fill"
                       VerticalAlignment="Top"
                       HorizontalAlignment="Center"
                       Height="170"
                       Width="170"
                       RenderTransformOrigin="0.5,0.5"
                       Margin="0,70,0,0"
                       Opacity="0.8" 
                       Visibility="{Binding MiniLogoVisibility}"/>
                <ScrollViewer HorizontalAlignment="Center" Margin="0,240,0,0" VerticalAlignment="Top" Height="40" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Visibility="{Binding MiniLogoVisibility}">
                    <TextBlock HorizontalAlignment="Center" Margin="0,0,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Text="{Binding FilterVM.FilterSummary}"/>
                </ScrollViewer>
                <!-- Search and Filter -->
                <Grid Visibility="{Binding DataContext.Main.CurrentPage,RelativeSource={RelativeSource AncestorType=Window}, 
                    Converter={StaticResource PageToVisibilityConverter},
                    ConverterParameter={x:Static presentation:Page.SearchAndFilter}}" d:IsHidden="True">
                    <Label Content="Search and Filter Cards" HorizontalAlignment="Center" VerticalAlignment="Top" FontSize="24" Height="40" Style="{StaticResource BelerenBold}"/>
                    <Label Visibility="{Binding AddCardsVM.StatusVisibility,Converter={StaticResource InverseVisibility}}"
                       HorizontalAlignment="Left" Margin="0,25,0,0" VerticalAlignment="Top" Height="30" >
                        <Label.Content>
                            <MultiBinding Converter="{StaticResource CountToSummaryConverter}">
                                <Binding Path="AllCardsVM.FilteredCount"/>
                                <Binding Path="AllCardsVM.TotalCount"/>
                            </MultiBinding>
                        </Label.Content>
                    </Label>
                    <Button Content="Submit these cards to my collection" HorizontalAlignment="Right" Margin="0,45,0,0" VerticalAlignment="Top" Width="200" Height="20" 
                            Visibility="{Binding AddCardsVM.CollectionEditVisibility}"
                            Command="{Binding AddCardsVM.SubmitNewCardsCommand}" />
                    <Button Content="Clear cards without submitting"
                        HorizontalAlignment="Right" Margin="0,45,210,0" VerticalAlignment="Top"
                        Width="200" Height="20"
                        Visibility="{Binding AddCardsVM.CollectionEditVisibility}"
                        Command="{Binding AddCardsVM.ClearCardsToAddCommand}" />
                    <Button Content="Add selected cards to my collection"
                        HorizontalAlignment="Right" Margin="0,255,0,0" VerticalAlignment="Top"
                        Width="200" Height="20"
                        Visibility="{Binding ElementName=AllCardsDataGrid, Path=SelectedItems.Count, Converter={StaticResource CountToVisibilityConverter}}"
                        Command="{Binding AddCardsVM.AddSelectedCardsCommand}"
                        CommandParameter="{Binding SelectedItems, ElementName=AllCardsDataGrid}" />
                    <ScrollViewer HorizontalAlignment="Left" Margin="0,40,0,0" VerticalAlignment="Top" Height="187" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                        <TextBlock TextWrapping="Wrap" VerticalAlignment="Top" Text="{Binding AddCardsVM.StatusMessage}"/>
                    </ScrollViewer>            
                    <!--Cards to add listview-->
                    <ListView DataContext="{Binding AddCardsVM}"
                                  ItemsSource="{Binding CardsToAdd}"
                                  Visibility="{Binding CollectionEditVisibility}"                          
                                  behaviors:ListViewColumnRefreshBehavior.RefreshTrigger="{Binding RefreshColumnsTrigger, Mode=OneWay}"                          
                                  Margin="0,70,0,0" VerticalAlignment="Top" Height="170">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Card Name" DisplayMemberBinding="{Binding Name}"/>
                                <GridViewColumn Header="Set" DisplayMemberBinding="{Binding SetName}"/>
                                <GridViewColumn Header="Condition">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <ComboBox ItemsSource="{Binding Conditions}"
                                                          SelectedItem="{Binding SelectedCondition, UpdateSourceTrigger=PropertyChanged}">
                                                <i:Interaction.Triggers>
                                                    <i:EventTrigger EventName="SelectionChanged">
                                                        <i:InvokeCommandAction Command="{Binding DataContext.RefreshColumnsCommand, RelativeSource={RelativeSource AncestorType=ListView}}"/>
                                                    </i:EventTrigger>
                                                </i:Interaction.Triggers>
                                            </ComboBox>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Finish">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <ComboBox ItemsSource="{Binding AvailableFinishes}" 
                                                          SelectedItem="{Binding SelectedFinish, UpdateSourceTrigger=PropertyChanged}">
                                                <i:Interaction.Triggers>
                                                    <i:EventTrigger EventName="SelectionChanged">
                                                        <i:InvokeCommandAction Command="{Binding DataContext.RefreshColumnsCommand, RelativeSource={RelativeSource AncestorType=ListView}}"/>
                                                    </i:EventTrigger>
                                                </i:Interaction.Triggers>
                                            </ComboBox>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Language">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <ComboBox ItemsSource="{Binding OtherLanguages}" 
                                                          SelectedItem="{Binding Language, UpdateSourceTrigger=PropertyChanged}">
                                                <i:Interaction.Triggers>
                                                    <i:EventTrigger EventName="SelectionChanged">
                                                        <i:InvokeCommandAction Command="{Binding DataContext.RefreshColumnsCommand, RelativeSource={RelativeSource AncestorType=ListView}}"/>
                                                    </i:EventTrigger>
                                                </i:Interaction.Triggers>
                                            </ComboBox>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="+">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="+" Width="15" Background="#FFFBF7B4"
                                                    Command="{Binding DataContext.IncrementCountCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                                    CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ListViewItem}}, Path=DataContext}" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Count">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBox HorizontalAlignment="Center" Background="White">
                                                <TextBox.Text>
                                                    <Binding Path="CardsOwned" Mode="TwoWay" UpdateSourceTrigger="Explicit" ValidatesOnDataErrors="True" NotifyOnValidationError="True">
                                                        <Binding.ValidationRules>
                                                            <validation:IntegerValidationRule />
                                                        </Binding.ValidationRules>
                                                    </Binding>
                                                </TextBox.Text>
                                                <i:Interaction.Behaviors>
                                                    <behaviors:DelayedUpdateBehavior Delay="500"/>
                                                    <behaviors:NumericValidationBehavior />
                                                </i:Interaction.Behaviors>
                                            </TextBox>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="-">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="-" Width="15" Background="#FFFBF7B4"
                                                    Command="{Binding DataContext.DecrementCountCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                                    CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ListViewItem}}, Path=DataContext}" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="+">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="+" Width="15" Background="#FFFBF7B4"
                                                    Command="{Binding DataContext.IncrementTradeCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                                    CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ListViewItem}}, Path=DataContext}" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Trade">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBox HorizontalAlignment="Center" Background="White">
                                                <TextBox.Text>
                                                    <Binding Path="CardsForTrade" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged"
                                                                 ValidatesOnDataErrors="True" NotifyOnValidationError="True">
                                                        <Binding.ValidationRules>
                                                            <validation:IntegerValidationRule />
                                                        </Binding.ValidationRules>
                                                    </Binding>
                                                </TextBox.Text>
                                                <i:Interaction.Behaviors>
                                                    <behaviors:NumericValidationBehavior />
                                                </i:Interaction.Behaviors>
                                            </TextBox>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="-">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="-" Width="15" Background="#FFFBF7B4"
                                                    Command="{Binding DataContext.DecrementTradeCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                                    CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ListViewItem}}, Path=DataContext}" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                    <!--The main card view datagrid-->
                    <DataGrid x:Name="AllCardsDataGrid"
                                  ItemsSource="{Binding AllCardsVM.FilteredCards, NotifyOnTargetUpdated=True}"
                                  Margin="0,280,0,0"
                                  AutoGenerateColumns="False"
                                  SelectedItem="{Binding CardImageVM.SelectedCard, Mode=TwoWay}"
                                  ScrollViewer.CanContentScroll="True"
                                  EnableColumnVirtualization="True"
                                  VirtualizingPanel.IsVirtualizing="True"
                                  CanUserAddRows="False"
                                  CanUserSortColumns="True"
                                  HorizontalAlignment="Stretch"
                                  IsReadOnly="True"
                                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                  behaviors:DataGridPersistSortBehavior.Enable="True"
                                  behaviors:DataGridColumnResizerBehavior.EnableAutoResize="True"
                                  behaviors:DataGridColumnResizerBehavior.DataGridIndex="0"
                                  behaviors:DataGridClearSelectionBehavior.ClearSelectionTrigger="{Binding AddCardsVM.ClearSelectionTrigger, Mode=TwoWay}">
                        <i:Interaction.Triggers>
                            <i:EventTrigger EventName="MouseDoubleClick">
                                <i:InvokeCommandAction 
                                    Command="{Binding AddCardsVM.AddSelectedCardsCommand}"
                                    CommandParameter="{Binding SelectedItems, ElementName=AllCardsDataGrid}" />
                            </i:EventTrigger>
                        </i:Interaction.Triggers>
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="Name" Width="*" SortMemberPath="Name">
                                <DataGridTemplateColumn.HeaderTemplate>
                                    <DataTemplate>
                                        <Grid DataContext="{Binding DataContext.Main.FilterVM.Filters[Name],RelativeSource={RelativeSource AncestorType=Window}}">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Label Content="Name" VerticalAlignment="Center" Grid.Column="0"/>
                                            <ComboBox IsTextSearchEnabled="False"
                                                          Grid.Column="1"
                                                          HorizontalAlignment="Stretch"
                                                          Height="20"
                                                          IsEditable="True"
                                                          Width="{Binding DataContext.Main.ColumnWidths[0][0],RelativeSource={RelativeSource AncestorType=Window}}"
                                                          ItemsSource="{Binding FilteredOptions}"
                                                          DisplayMemberPath="OptionName"
                                                          SelectedItem="OptionName"
                                                          Text="{Binding FreetextSearch, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                                                <ComboBox.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <VirtualizingStackPanel />
                                                    </ItemsPanelTemplate>
                                                </ComboBox.ItemsPanel>
                                            </ComboBox>
                                        </Grid>
                                    </DataTemplate>
                                </DataGridTemplateColumn.HeaderTemplate>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Name}" TextWrapping="Wrap" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Set" Width="*" SortMemberPath="SetName">
                                <DataGridTemplateColumn.HeaderTemplate>
                                    <DataTemplate>
                                        <Grid DataContext="{Binding DataContext.Main.FilterVM.Filters[SetName],RelativeSource={RelativeSource AncestorType=Window}}">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Label Content="Set" VerticalAlignment="Center" Grid.Column="0"/>
                                            <ComboBox IsTextSearchEnabled="False"
                                                          Grid.Column="1"
                                                          HorizontalAlignment="Stretch"
                                                          Height="20"
                                                          IsEditable="True"
                                                          Width="{Binding DataContext.Main.ColumnWidths[0][1],RelativeSource={RelativeSource AncestorType=Window}}"
                                                          ItemsSource="{Binding FilteredOptions}"
                                                          DisplayMemberPath="OptionName"
                                                          SelectedItem="OptionName"
                                                          Text="{Binding FreetextSearch, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                                                <ComboBox.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <VirtualizingStackPanel />
                                                    </ItemsPanelTemplate>
                                                </ComboBox.ItemsPanel>
                                            </ComboBox>
                                        </Grid>
                                    </DataTemplate>
                                </DataGridTemplateColumn.HeaderTemplate>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding SetName}" TextWrapping="Wrap" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Icon" IsReadOnly="True" Width="Auto" SortMemberPath="ReleaseDate">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Image Source="{Binding KeyRuneImage}" Width="20" Height="20" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Mana Cost" IsReadOnly="True" Width="Auto" SortMemberPath="ManaValue">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Image Source="{Binding ManaCostImage}" Height="20" HorizontalAlignment="Right"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Card Types" Width="*" SortMemberPath="Type">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Type}" TextWrapping="Wrap" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Finishes" Width="*">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Finishes}" TextWrapping="Wrap" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn SortMemberPath="NormalPrice" Width="*">
                                <DataGridTemplateColumn.HeaderTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding DataContext.Main.PricesVM.PriceHeader, RelativeSource={RelativeSource AncestorType=Window}}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.HeaderTemplate>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding NormalPrice}" TextWrapping="Wrap"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn SortMemberPath="FoilPrice" Width="*">
                                <DataGridTemplateColumn.HeaderTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding DataContext.Main.PricesVM.FoilPriceHeader, RelativeSource={RelativeSource AncestorType=Window}}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.HeaderTemplate>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding FoilPrice}" TextWrapping="Wrap"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn SortMemberPath="EtchedPrice" Width="*">
                                <DataGridTemplateColumn.HeaderTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding DataContext.Main.PricesVM.EtchedPriceHeader, RelativeSource={RelativeSource AncestorType=Window}}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.HeaderTemplate>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding EtchedPrice}" TextWrapping="Wrap"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                        <DataGrid.ContextMenu>
                            <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                                <MenuItem Header="Add selected card(s) to Collection"
                                              Command="{Binding AddCardsVM.AddSelectedCardsCommand}"
                                              CommandParameter="{Binding PlacementTarget.SelectedItems, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              Style="{StaticResource BelerenBold}" />
                                <MenuItem Header="Add selected card(s) with DEFAULT values to Collection" 
                                              Command="{Binding AddCardsVM.SubmitNewCardsWithDefaultsCommand}"
                                              CommandParameter="{Binding PlacementTarget.SelectedItems, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              Style="{StaticResource BelerenBold}"/>
                                <MenuItem Header="">
                                    <MenuItem.Template>
                                        <ControlTemplate>
                                            <Label Content="Default values are:" Margin="30,0,0,0"/>
                                        </ControlTemplate>
                                    </MenuItem.Template>
                                </MenuItem>
                                <MenuItem Header="">
                                    <MenuItem.Template>
                                        <ControlTemplate>
                                            <Label Content=" - Condition: Near Mint" Margin="30,0,0,0"/>
                                        </ControlTemplate>
                                    </MenuItem.Template>
                                </MenuItem>
                                <MenuItem Header="">
                                    <MenuItem.Template>
                                        <ControlTemplate>
                                            <Label Content=" - Language: English" Margin="30,0,0,0"/>
                                        </ControlTemplate>
                                    </MenuItem.Template>
                                </MenuItem>
                                <MenuItem Header="">
                                    <MenuItem.Template>
                                        <ControlTemplate>
                                            <Label Content=" - Finish: nonfoil" Margin="30,0,0,0"/>
                                        </ControlTemplate>
                                    </MenuItem.Template>
                                </MenuItem>
                                <MenuItem Header="">
                                    <MenuItem.Template>
                                        <ControlTemplate>
                                            <Label Content=" - Count: 1" Margin="30,0,0,0"/>
                                        </ControlTemplate>
                                    </MenuItem.Template>
                                </MenuItem>
                                <MenuItem Header="">
                                    <MenuItem.Template>
                                        <ControlTemplate>
                                            <Label Content=" - Trade: 0" Margin="30,0,0,0"/>
                                        </ControlTemplate>
                                    </MenuItem.Template>
                                </MenuItem>
                            </ContextMenu>
                        </DataGrid.ContextMenu>
                    </DataGrid>
                </Grid>
                <!-- My Collection -->
                <Grid Visibility="{Binding DataContext.Main.CurrentPage,RelativeSource={RelativeSource AncestorType=Window},Converter={StaticResource PageToVisibilityConverter},ConverterParameter={x:Static presentation:Page.MyCollection}}">
                    <Label Content="My Collection" HorizontalAlignment="Center" VerticalAlignment="Top" FontSize="24" Height="40" Style="{StaticResource BelerenBold}"/>
                    <Label HorizontalAlignment="Left" Margin="0,25,0,0" VerticalAlignment="Top" Height="30" Visibility="{Binding EditCardsVM.StatusVisibility,Converter={StaticResource InverseVisibility}}">
                        <Label.Content>
                            <MultiBinding Converter="{StaticResource CountToSummaryConverter}">
                                <Binding Path="MyCollectionVM.FilteredCount"/>
                                <Binding Path="MyCollectionVM.TotalCount"/>
                            </MultiBinding>
                        </Label.Content>
                    </Label>
                    <Button Content="Update selected cards" HorizontalAlignment="Right" Margin="0,45,0,0" VerticalAlignment="Top" Width="200" Height="20"
                            Visibility="{Binding EditCardsVM.CollectionEditVisibility}"
                            Command="{Binding EditCardsVM.SubmitCardEditsCommand}" />
                    <Button Content="Clear cards without submitting"
                            HorizontalAlignment="Right" Margin="0,45,210,0" VerticalAlignment="Top"
                            Width="200" Height="20"
                            Visibility="{Binding EditCardsVM.CollectionEditVisibility}"
                            Command="{Binding EditCardsVM.ClearCardsToAddCommand}" />
                    <Button Content="Edit selected cards"
                        HorizontalAlignment="Right" Margin="0,255,0,0" VerticalAlignment="Top"
                        Width="120" Height="20"
                        Visibility="{Binding ElementName=MyCollectionDataGrid, Path=SelectedItems.Count, Converter={StaticResource CountToVisibilityConverter}}"
                        Command="{Binding EditCardsVM.EditSelectedCardsCommand}"
                        CommandParameter="{Binding SelectedItems, ElementName=MyCollectionDataGrid}" />
                    <ScrollViewer HorizontalAlignment="Left" Margin="0,40,0,0" VerticalAlignment="Top" Height="187" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                        <TextBlock Text="{Binding EditCardsVM.StatusMessage}" TextWrapping="Wrap" VerticalAlignment="Top" />
                    </ScrollViewer>

                    <!--Cards to edit-->
                    <ListView DataContext="{Binding EditCardsVM}"
                              ItemsSource="{Binding CardsToAdd}"
                              Visibility="{Binding CollectionEditVisibility}"                          
                              behaviors:ListViewColumnRefreshBehavior.RefreshTrigger="{Binding RefreshColumnsTrigger, Mode=OneWay}"    
                              Margin="0,70,0,0" VerticalAlignment="Top" Height="170">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Card Name" DisplayMemberBinding="{Binding Name}"/>
                                <GridViewColumn Header="Set" DisplayMemberBinding="{Binding SetName}"/>
                                <GridViewColumn Header="Condition">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <ComboBox ItemsSource="{Binding Conditions}"
                                                      SelectedItem="{Binding SelectedCondition, UpdateSourceTrigger=PropertyChanged}">
                                                <i:Interaction.Triggers>
                                                    <i:EventTrigger EventName="SelectionChanged">
                                                        <i:InvokeCommandAction Command="{Binding DataContext.RefreshColumnsCommand, RelativeSource={RelativeSource AncestorType=ListView}}"/>
                                                    </i:EventTrigger>
                                                </i:Interaction.Triggers>
                                            </ComboBox>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Finish">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <ComboBox ItemsSource="{Binding AvailableFinishes}" 
                                                      SelectedItem="{Binding SelectedFinish, UpdateSourceTrigger=PropertyChanged}">
                                                <i:Interaction.Triggers>
                                                    <i:EventTrigger EventName="SelectionChanged">
                                                        <i:InvokeCommandAction Command="{Binding DataContext.RefreshColumnsCommand, RelativeSource={RelativeSource AncestorType=ListView}}"/>
                                                    </i:EventTrigger>
                                                </i:Interaction.Triggers>
                                            </ComboBox>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Language">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <ComboBox ItemsSource="{Binding OtherLanguages}" 
                                                      SelectedItem="{Binding Language, UpdateSourceTrigger=PropertyChanged}">
                                                <i:Interaction.Triggers>
                                                    <i:EventTrigger EventName="SelectionChanged">
                                                        <i:InvokeCommandAction Command="{Binding DataContext.RefreshColumnsCommand, RelativeSource={RelativeSource AncestorType=ListView}}"/>
                                                    </i:EventTrigger>
                                                </i:Interaction.Triggers>
                                            </ComboBox>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="+">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="+" Width="15" Background="#FFFBF7B4"
                                                Command="{Binding DataContext.IncrementCountCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                                CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ListViewItem}}, Path=DataContext}" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Count">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBox HorizontalAlignment="Center" Background="White">
                                                <TextBox.Text>
                                                    <Binding Path="CardsOwned" Mode="TwoWay" UpdateSourceTrigger="Explicit" ValidatesOnDataErrors="True" NotifyOnValidationError="True">
                                                        <Binding.ValidationRules>
                                                            <validation:IntegerValidationRule />
                                                        </Binding.ValidationRules>
                                                    </Binding>
                                                </TextBox.Text>
                                                <i:Interaction.Behaviors>
                                                    <behaviors:DelayedUpdateBehavior Delay="500"/>
                                                    <behaviors:NumericValidationBehavior />
                                                </i:Interaction.Behaviors>
                                            </TextBox>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="-">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="-" Width="15" Background="#FFFBF7B4"
                                                Command="{Binding DataContext.DecrementCountCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                                CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ListViewItem}}, Path=DataContext}" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="+">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="+" Width="15" Background="#FFFBF7B4"
                                                Command="{Binding DataContext.IncrementTradeCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                                CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ListViewItem}}, Path=DataContext}" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="Trade">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBox HorizontalAlignment="Center" Background="White">
                                                <TextBox.Text>
                                                    <Binding Path="CardsForTrade" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged"
                                                             ValidatesOnDataErrors="True" NotifyOnValidationError="True">
                                                        <Binding.ValidationRules>
                                                            <validation:IntegerValidationRule />
                                                        </Binding.ValidationRules>
                                                    </Binding>
                                                </TextBox.Text>
                                                <i:Interaction.Behaviors>
                                                    <behaviors:NumericValidationBehavior />
                                                </i:Interaction.Behaviors>
                                            </TextBox>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Header="-">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="-" Width="15" Background="#FFFBF7B4"
                                                Command="{Binding DataContext.DecrementTradeCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                                CommandParameter="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type ListViewItem}}, Path=DataContext}" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                    <!--The my collection datagrid-->
                    <DataGrid x:Name="MyCollectionDataGrid"
                              ItemsSource="{Binding MyCollectionVM.FilteredCards, NotifyOnTargetUpdated=True}"
                              Margin="0,280,0,0"
                              AutoGenerateColumns="False"
                              SelectedItem="{Binding CardImageVM.SelectedCard, Mode=TwoWay}"
                              ScrollViewer.CanContentScroll="True"
                              EnableColumnVirtualization="True"
                              VirtualizingPanel.IsVirtualizing="True"
                              CanUserAddRows="False"
                              CanUserSortColumns="True"
                              HorizontalAlignment="Stretch"
                              ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                              behaviors:DataGridPersistSortBehavior.Enable="True"
                              behaviors:DataGridColumnResizerBehavior.EnableAutoResize="True"
                              behaviors:DataGridColumnResizerBehavior.DataGridIndex="1"                              
                              behaviors:DataGridColumnResizerBehavior.UpdateOnToken="{Binding DataContext.Main.MyCollectionResizeToken, RelativeSource={RelativeSource AncestorType=Window}}"
                              behaviors:DataGridClearSelectionBehavior.ClearSelectionTrigger="{Binding EditCardsVM.ClearSelectionTrigger, Mode=TwoWay}">
                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="MouseDoubleClick">
                                        <i:InvokeCommandAction Command="{Binding EditCardsVM.EditSelectedCardsCommand}"
                                                  CommandParameter="{Binding SelectedItems, ElementName=MyCollectionDataGrid}" />
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="Name" Width="*" SortMemberPath="Name">
                                <DataGridTemplateColumn.HeaderTemplate>
                                    <DataTemplate>
                                        <Grid DataContext="{Binding DataContext.Main.FilterVM.Filters[Name],RelativeSource={RelativeSource AncestorType=Window}}">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Label Content="Name" VerticalAlignment="Center" Grid.Column="0"/>
                                            <ComboBox IsTextSearchEnabled="False"
                                                      Grid.Column="1"
                                                      HorizontalAlignment="Stretch"
                                                      Height="20"
                                                      IsEditable="True"
                                                      Width="{Binding DataContext.Main.ColumnWidths[1][0],RelativeSource={RelativeSource AncestorType=Window}}"
                                                      ItemsSource="{Binding FilteredOptions}"
                                                      DisplayMemberPath="OptionName"
                                                      SelectedItem="OptionName"
                                                      Text="{Binding FreetextSearch, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                                                <ComboBox.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <VirtualizingStackPanel />
                                                    </ItemsPanelTemplate>
                                                </ComboBox.ItemsPanel>
                                            </ComboBox>
                                        </Grid>
                                    </DataTemplate>
                                </DataGridTemplateColumn.HeaderTemplate>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Name}" TextWrapping="Wrap" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Set" Width="*" SortMemberPath="SetName">
                                <DataGridTemplateColumn.HeaderTemplate>
                                    <DataTemplate>
                                        <Grid DataContext="{Binding DataContext.Main.FilterVM.Filters[SetName],RelativeSource={RelativeSource AncestorType=Window}}">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Label Content="Set" VerticalAlignment="Center" Grid.Column="0"/>
                                            <ComboBox IsTextSearchEnabled="False"
                                                      Grid.Column="1"
                                                      HorizontalAlignment="Stretch"
                                                      Height="20"
                                                      IsEditable="True"
                                                      Width="{Binding DataContext.Main.ColumnWidths[1][1],RelativeSource={RelativeSource AncestorType=Window}}"
                                                      ItemsSource="{Binding FilteredOptions}"
                                                      DisplayMemberPath="OptionName"
                                                      SelectedItem="OptionName"
                                                      Text="{Binding FreetextSearch, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                                                <ComboBox.ItemsPanel>
                                                    <ItemsPanelTemplate>
                                                        <VirtualizingStackPanel />
                                                    </ItemsPanelTemplate>
                                                </ComboBox.ItemsPanel>
                                            </ComboBox>
                                        </Grid>
                                    </DataTemplate>
                                </DataGridTemplateColumn.HeaderTemplate>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding SetName}" TextWrapping="Wrap" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Icon" IsReadOnly="True" Width="Auto" SortMemberPath="ReleaseDate">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Image Source="{Binding KeyRuneImage}" Width="20" Height="20" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Mana Cost" Width="Auto" IsReadOnly="True" SortMemberPath="ManaValue">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Image Source="{Binding ManaCostImage}" Height="20" HorizontalAlignment="Right"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Card Types" Width="*" SortMemberPath="Type">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Type}" TextWrapping="Wrap" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Finish" Width="*" SortMemberPath="SelectedFinish">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding SelectedFinish}" TextWrapping="Wrap" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Language" Width="*" SortMemberPath="Language">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Language}" TextWrapping="Wrap" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Condition" Width="*" SortMemberPath="SelectedCondition">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding SelectedCondition}" TextWrapping="Wrap" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Count" Width="*" SortMemberPath="CardsOwned">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding CardsOwned}" TextWrapping="Wrap"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Trade" Width="*" SortMemberPath="CardsForTrade">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding CardsForTrade}" TextWrapping="Wrap" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Price" Width="*" SortMemberPath="CardInCollectionPrice">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding CardInCollectionPrice}" TextWrapping="Wrap" />
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                        <DataGrid.ContextMenu>
                            <ContextMenu DataContext="{Binding PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                                <MenuItem Header="Edit selected card(s)" Style="{StaticResource BelerenBold}"
                                          Command="{Binding EditCardsVM.AddSelectedCardsCommand}"
                                          CommandParameter="{Binding PlacementTarget.SelectedItems, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                                <MenuItem Header="UPDATE selected card(s) - put ALL for TRADE" Style="{StaticResource BelerenBold}"
                                          Command="{Binding EditCardsVM.PutAllForTradeCommand}"
                                          CommandParameter="{Binding PlacementTarget.SelectedItems, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>                            
                                <MenuItem Header="UPDATE selected card(s) - put NONE for TRADE" Style="{StaticResource BelerenBold}"
                                          Command="{Binding EditCardsVM.SetNoneForTradeCommand}"
                                          CommandParameter="{Binding PlacementTarget.SelectedItems, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>                            
                                <MenuItem Header="DELETE card(s) from collection" Style="{StaticResource BelerenBold}"
                                          Command="{Binding EditCardsVM.DeleteSelectedCardsCommand}"
                                          CommandParameter="{Binding PlacementTarget.SelectedItems, RelativeSource={RelativeSource AncestorType=ContextMenu}}"/>
                            </ContextMenu>
                        </DataGrid.ContextMenu>
                    </DataGrid>
                </Grid>
                <Grid x:Name="GridDecks" d:IsHidden="True"
                      Visibility="{Binding DataContext.Main.CurrentPage,RelativeSource={RelativeSource AncestorType=Window},
                   Converter={StaticResource PageToVisibilityConverter},
                   ConverterParameter={x:Static presentation:Page.Decks}}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="40"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0" VerticalAlignment="Top" HorizontalAlignment="Center">
                        <Label x:Name="HeadlineDecks" Content="Deck Management" HorizontalAlignment="Center" VerticalAlignment="Top" FontSize="24" Height="40" Style="{StaticResource BelerenBold}"/>
                    </Grid>
                    <Grid x:Name="GridDecksOverview" Grid.Row="1" VerticalAlignment="Top" d:IsHidden="True">
                        <Button x:Name="AddDeckButton" Width="100" Content="Add a new deck" Height="20" Margin="0,95,500,0" HorizontalAlignment="Center" VerticalAlignment="Top" Click="AddDeckButton_Click" d:IsHidden="True" />
                        <Grid x:Name="GridAddNewDeckForm" VerticalAlignment="Top" HorizontalAlignment="Center" Width="600" Margin="0,00,0,0" Visibility="Collapsed">
                            <Label HorizontalAlignment="Left" VerticalAlignment="Top" Content="Deck Name:" Height="25" />
                            <TextBox x:Name="AddDeckNameTextBox" Width="305" Height="20" Margin="160,0,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" Background="White" />
                            <Label HorizontalAlignment="Left" VerticalAlignment="Top" Content="Deck Desciption (optional):" Margin="0,30,0,0" Height="25" />
                            <TextBox x:Name="AddDeckDescriptionTextBox" Width="305" Height="40" Margin="160,30,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" Background="White" TextWrapping="Wrap" />
                            <Label HorizontalAlignment="Left" VerticalAlignment="Top" Content="Select a format (optional):" Margin="0,80,0,0" Height="25" />
                            <ComboBox x:Name="NewDeckFormatComboBox" Width="200" Margin="160,80,0,0" Height="20" HorizontalAlignment="Left" VerticalAlignment="Top" />
                            <Button x:Name="SubmitNewDeckButton" Width="100" Content="Submit new deck" Height="20" Margin="365,80,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" Click="SubmitNewDeckButton_Click" />
                            <Button x:Name="CancelNewDeckButton" Content="Cancel" Height="20" Margin="470,80,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" Width="45" Click="CancelNewDeckButton_Click"/>
                        </Grid>
                        <ListView x:Name="MyDecksListView" HorizontalAlignment="Center" Margin="0,120,0,0" SelectionMode="Single" Width="600" ScrollViewer.HorizontalScrollBarVisibility="Disabled" MouseDoubleClick="OpenAndEditDeck">
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                </Style>
                            </ListView.ItemContainerStyle>
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="Deck Name" Width="150">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding DeckName}" TextWrapping="Wrap"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="Description" Width="240">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Description}" TextWrapping="Wrap"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="Target Format" Width="140">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding TargetFormat}"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="Action" Width="45">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal">
                                                    <Button Content="&#xE104;" FontFamily="Segoe MDL2 Assets" ToolTip="Edit deck" Margin="0,0,4,0" VerticalAlignment="Center" HorizontalAlignment="Center" Click="OpenAndEditDeck"/>
                                                    <Button Content="&#xE107;" FontFamily="Segoe MDL2 Assets" ToolTip="Delete" VerticalAlignment="Center" HorizontalAlignment="Center" Click="DeleteDeckButton_Click"/>
                                                </StackPanel>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </Grid>
                    <Grid x:Name="GridDeckEditor" Grid.Row="1" VerticalAlignment="Top" Visibility="Hidden">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="100"/>
                            <RowDefinition Height="150"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0" HorizontalAlignment="Center" Margin="0,-2,0,0">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,300,0" VerticalAlignment="Top" >
                                <TextBox x:Name="DeckNameTextBox" TextWrapping="Wrap" Text="Deck name" Width="300" FontSize="18" Background="{x:Null}" BorderBrush="{x:Null}" Height="25" IsReadOnly="True" MouseDoubleClick="EditDeckInfoButton_Click" KeyDown="DeckInfoTextBox_KeyDown"/>
                                <Button x:Name="EditDeckNameButton" Content="&#xE104;" FontFamily="Segoe MDL2 Assets" ToolTip="Edit deck name" VerticalAlignment="Center" HorizontalAlignment="Center" Click="EditDeckInfoButton_Click"/>
                                <Button x:Name="SaveDeckNameButton" Content="&#xE105;" FontFamily="Segoe MDL2 Assets" ToolTip="Save deck name" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Hidden" Click="SaveDeckInfoButton_Click"/>
                                <Button x:Name="CancelDeckNameEditButton" Content="&#xE10A;" FontFamily="Segoe MDL2 Assets" ToolTip="Cancel" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Hidden" Click="CancelDeckEditButton_Click"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,30,300,0" VerticalAlignment="Top" >
                                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Height="30" Width="300" HorizontalAlignment="Left">
                                    <TextBox x:Name="DeckDescriptionTextBox" TextWrapping="Wrap" Text="Deck description" VerticalAlignment="Top" Width="280" Background="{x:Null}" BorderBrush="{x:Null}" IsReadOnly="True" MouseDoubleClick="EditDeckInfoButton_Click" KeyDown="DeckInfoTextBox_KeyDown" HorizontalAlignment="Left"/>
                                </ScrollViewer>
                                <Button x:Name="EditDeckDescriptionButton" Content="&#xE104;" FontFamily="Segoe MDL2 Assets" ToolTip="Edit deck description" VerticalAlignment="Center" Click="EditDeckInfoButton_Click"/>
                                <Button x:Name="SaveDeckDescriptionButton" Content="&#xE105;" FontFamily="Segoe MDL2 Assets" ToolTip="Save deck description" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Hidden" Click="SaveDeckInfoButton_Click"/>
                                <Button x:Name="CancelDeckDescriptionEditButton" Content="&#xE10A;" FontFamily="Segoe MDL2 Assets" ToolTip="Cancel" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Hidden" Click="CancelDeckEditButton_Click"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,60,300,0" VerticalAlignment="Top" >
                                <TextBox x:Name="DeckFormatTextBox" TextWrapping="Wrap" Text="Deck format" VerticalAlignment="Top" Width="300" Background="{x:Null}" BorderBrush="{x:Null}" IsReadOnly="True" MouseDoubleClick="EditDeckInfoButton_Click"/>
                                <ComboBox x:Name="ExistingDeckFormatComboBox" Height="20" VerticalAlignment="Top" Width="300" Visibility="Collapsed"/>
                                <Button x:Name="EditDeckFormatButton" Content="&#xE104;" FontFamily="Segoe MDL2 Assets" ToolTip="Edit target format" VerticalAlignment="Center" Click="EditDeckInfoButton_Click"/>
                                <Button x:Name="SaveDeckFormatButton" Content="&#xE105;" FontFamily="Segoe MDL2 Assets" ToolTip="Save target format" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Hidden" Click="SaveDeckInfoButton_Click"/>
                                <Button x:Name="CancelDeckFormatEditButton" Content="&#xE10A;" FontFamily="Segoe MDL2 Assets" ToolTip="Cancel" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Hidden" Click="CancelDeckEditButton_Click"/>
                            </StackPanel>
                            <Button x:Name="BackToDeckOverviewButton" Content="Back" HorizontalAlignment="Right" VerticalAlignment="Top" Click="BackToDeckOverviewButton_Click" />
                        </Grid>
                        <Grid Grid.Row="1">
                            <DataGrid x:Name="AllCardsForDecksDataGrid"
                                      ItemsSource="{Binding AllCardsForDecksVM.FilteredCards}"
                                      AutoGenerateColumns="False"
                                      MouseDoubleClick="AddCardToDeck"
                                      ScrollViewer.CanContentScroll="True"
                                      EnableColumnVirtualization="True"
                                      VirtualizingPanel.IsVirtualizing="True"
                                      CanUserAddRows="False"
                                      CanUserSortColumns="True"
                                      ScrollViewer.HorizontalScrollBarVisibility="Disabled" Margin="0,129,0,-129"
                                      behaviors:DataGridColumnResizerBehavior.EnableAutoResize="True"
                                      behaviors:DataGridColumnResizerBehavior.DataGridIndex="2">
                                <DataGrid.Columns>
                                    <DataGridTemplateColumn Header="Name" Width="*" SortMemberPath="Name">
                                        <DataGridTemplateColumn.HeaderTemplate>
                                            <DataTemplate>
                                                <Grid DataContext="{Binding DataContext.Main.FilterVM.Filters[Name],RelativeSource={RelativeSource AncestorType=Window}}">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    <Label Content="Name" VerticalAlignment="Center" Grid.Column="0"/>
                                                    <ComboBox IsTextSearchEnabled="False"
                                                      Grid.Column="1"
                                                      HorizontalAlignment="Stretch"
                                                      Height="20"
                                                      IsEditable="True"
                                                      Width="{Binding DataContext.Main.ColumnWidths[2][0],RelativeSource={RelativeSource AncestorType=Window}}"
                                                      ItemsSource="{Binding FilteredOptions}"
                                                      DisplayMemberPath="OptionName"
                                                      SelectedItem="OptionName"
                                                      Text="{Binding FreetextSearch, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                                                        <ComboBox.ItemsPanel>
                                                            <ItemsPanelTemplate>
                                                                <VirtualizingStackPanel />
                                                            </ItemsPanelTemplate>
                                                        </ComboBox.ItemsPanel>
                                                    </ComboBox>
                                                </Grid>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.HeaderTemplate>
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Name}" TextWrapping="Wrap" />
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                    <DataGridTemplateColumn Header="Mana Cost" IsReadOnly="True" Width="Auto" SortMemberPath="ManaValue">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Image Source="{Binding ManaCostImage}" Height="20" HorizontalAlignment="Right"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                    <DataGridTemplateColumn Header="Card Supertypes" Width="*" SortMemberPath="SuperTypes">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding SuperTypes}" TextWrapping="Wrap" />
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                    <DataGridTemplateColumn Header="Card Types" Width="*" SortMemberPath="Types">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Types}" TextWrapping="Wrap" />
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                    <DataGridTemplateColumn Header="Card Subtypes" Width="*" SortMemberPath="SubTypes">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding SubTypes}" TextWrapping="Wrap" />
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                        <Grid Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,14,0,0">
                            <DataGrid x:Name="DeckDataGrid"
                                      AutoGenerateColumns="False"         
                                      ScrollViewer.CanContentScroll="True"
                                      EnableColumnVirtualization="True"
                                      VirtualizingPanel.IsVirtualizing="True"
                                      CanUserAddRows="False"
                                      CanUserSortColumns="True"
                                      ScrollViewer.HorizontalScrollBarVisibility="Disabled" Margin="0,126,0,-126">
                                <DataGrid.Columns>
                                    <DataGridTemplateColumn Header="Card Name" Width="*" SortMemberPath="Name">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Name}" TextWrapping="Wrap" />
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                    <DataGridTemplateColumn Header="Mana Cost" IsReadOnly="True" Width="Auto" SortMemberPath="ManaValue">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Image Source="{Binding ManaCostImage}" Height="20" HorizontalAlignment="Right"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                    <DataGridTemplateColumn Header="Card Type" Width="*" SortMemberPath="Type">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Type}" TextWrapping="Wrap" />
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                    <DataGridTemplateColumn Header="Count" Width="*" SortMemberPath="Count">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Count}" TextWrapping="Wrap" />
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Grid>
                </Grid>
                <Grid x:Name="GridUtilitiesSection" d:IsHidden="True"
                      Visibility="{Binding DataContext.Main.CurrentPage,RelativeSource={RelativeSource AncestorType=Window},
                    Converter={StaticResource PageToVisibilityConverter},
                    ConverterParameter={x:Static presentation:Page.Utilities}}">
                    <Label x:Name="HeadlineUtilities" Content="Utilities" HorizontalAlignment="Center" VerticalAlignment="Top" FontSize="24" Height="40" Style="{StaticResource BelerenBold}"/>
                    <Label x:Name="UtilsInfoLabel" Content="" HorizontalAlignment="Center" Margin="8,150,0,0" VerticalAlignment="Top" FontSize="16"/>
                    <Image x:Name="Inspiredtinkering" Margin="0,200,0,0" Source="/Resources/Images/inspired-tinkering-183.png" HorizontalAlignment="Center" VerticalAlignment="Top" Width="600" Opacity="0.698"/>
                    <Grid x:Name="GridImportWizard" Visibility="Collapsed" d:IsHidden="True">
                        <Label x:Name="ImportStartLabel" Content="The Import Wizard" HorizontalAlignment="Center" Margin="0,45,0,0" VerticalAlignment="Top" FontSize="48" Style="{StaticResource BelerenBold}"/>
                        <Button x:Name="ButtonCancelImport" Content="Cancel Import" HorizontalAlignment="Center" Margin="0,350,410,0" VerticalAlignment="Top" Width="90" Height="20" Visibility="Collapsed" Click="CancelImport_Click"/>
                        <Label x:Name="CrunchingDataLabel" Content="Please wait - crunching data..." HorizontalAlignment="Center" Margin="0,350,0,0" VerticalAlignment="Top" Height="25" Width="170" Visibility="Collapsed"/>
                        <Grid x:Name="GridImportStartScreen" HorizontalAlignment="Center" Visibility="Collapsed">
                            <TextBlock HorizontalAlignment="Center" Margin="0,146,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Height="280" FontSize="16">
                                <Run Text="You have chosen to consult the Import Wizard. This will allow you to import card lists to your collection. "/>
                                <LineBreak/><LineBreak/>
                                <Run Text="Some important notes:"/><LineBreak/>
                                <Run Text=" &#x2022; Card list has to be in a .csv format"/><LineBreak/>
                                <Run Text=" &#x2022; Cards imported with the wizard will be ADDED to existing cards in your collection"/><LineBreak/>
                                <Run Text=" &#x2022; Imported cards will be mapped to a specific version of the card"/>
                                <LineBreak/><LineBreak/>
                                <Run Text="Elborating on the last point: As an example, there are 4 different versions of the card &quot;Flooded Strand&quot; in the Modern Horizons 3. If your card list contains:"/>
                                <LineBreak/><LineBreak/>
                                <Run Text=" &#x2022; | Flooded Strand | Modern Horizons 3 |"/>
                                <LineBreak/><LineBreak/>
                                <Run Text="...we need to figure out which specific version of &quot;Flooded Strand&quot; should be added to your collection."/><LineBreak/>
                            </TextBlock>
                            <Image Margin="0,420,0,0" Source="/Resources/Images/rush_of_knowledge.jpg" HorizontalAlignment="Center" VerticalAlignment="Top" Height="274" Width="344" Opacity="0.7"/>
                            <Button x:Name="BeginImportButton" Content="Let's go!" HorizontalAlignment="Center" Margin="0,720,0,0" VerticalAlignment="Top" Height="20" Click="BeginImportButton_Click" Width="74"/>
                        </Grid>
                        <Grid x:Name="GridImportIdColumnMapping" Visibility="Collapsed">
                            <Label Content="Does you import file have an ID column?" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,140,0,0" FontSize="24" Style="{StaticResource BelerenBold}"/>
                            <TextBlock Margin="0,184,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Height="140" FontSize="16" HorizontalAlignment="Center">
                                <Run Text="If your csv-file has any column with card Id, The Import Wizard will have the greatest chance of succesfully mapping your cards to be imported with cards in the main card database."/>
                                <LineBreak/><LineBreak/>
                                <Run Text="It will recognize a variety of card id formats, e.g. Scryfall Id, Card Market Id, TCG Player Id etc."/><LineBreak/>
                                <Run Text=" &#x2022; In the "/><Run FontWeight="Bold" Text="left"/><Run Text=" column, choose the card Id header in your import file"/><LineBreak/>
                                <Run Text=" &#x2022; In the "/><Run FontWeight="Bold" Text="right"/><Run Text=" column, choose the matching format available in the card database"/><LineBreak/>
                                <Run Text=" &#x2022; If your import file does not contain a card id column, simply click &quot;Skip&quot;"/>
                            </TextBlock>
                            <ListView x:Name="IdColumnMappingListView" Margin="0,380,0,0" Width="500" Height="77" HorizontalAlignment="Center" VerticalAlignment="Top">
                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn Header="Import file card Id header" Width="150">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <ComboBox ItemsSource="{Binding CsvHeaders}" SelectedItem="{Binding SelectedCsvHeader}" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn Header="Matching database card Id format" Width="200">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <ComboBox ItemsSource="{Binding DatabaseFields}" SelectedItem="{Binding SelectedDatabaseField}" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn Header="Reset mapping" Width="100">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Content="Clear Mapping" Width="85" Click="ClearMappingButton_Click"/>
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                    </GridView>
                                </ListView.View>
                            </ListView>
                            <Button x:Name="ButtonIdColumnMappingNext" Content="Next" HorizontalAlignment="Center" Margin="410,350,0,0" VerticalAlignment="Top" Width="90" Height="20" Click="ButtonIdColumnMappingNext_Click"/>
                            <Button x:Name="ButtonSkipIdColumnMapping" Content="Skip" HorizontalAlignment="Center" Margin="555,380,0,0" VerticalAlignment="Top" Width="30" Height="20" Click="ButtonSkipIdColumnMapping_Click"/>
                        </Grid>
                        <Grid x:Name="GridImportNameAndSetMapping" Visibility="Collapsed">
                            <Label Content="Card Name, Set Name and Set Code Mapping" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,140,0,0" FontSize="24" Style="{StaticResource BelerenBold}"/>
                            <TextBlock HorizontalAlignment="Center" Margin="0,184,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Height="140" FontSize="16">
                                <Run Text="If you are, it means that The Import Wizard was not able to map all cards in import file uniquely to a card in the card database."/>
                                <LineBreak/><LineBreak/>
                                <Run Text="Fret not, bold mage! Now we will try to map the cards in the import file using the card name + set name and/or set code, assuming they exist in the import file."/><LineBreak/>
                                <Run Text="You must choose a mapping for card name and at least one of either set name or set code. If you can map both, it will try to match using both."/>
                            </TextBlock>
                            <ListView x:Name="NameAndSetMappingListView" Margin="0,380,0,0" Width="500" Height="110" HorizontalAlignment="Center" VerticalAlignment="Top">
                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn Header="Fields to map" Width="150">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding CardSetField}" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn Header="Import file header" Width="200">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <ComboBox ItemsSource="{Binding CsvHeaders}" SelectedItem="{Binding CsvHeader}" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn Header="Reset Mapping" Width="100">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Content="Clear Mapping" Width="85" Click="ClearMappingButton_Click"/>
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                    </GridView>
                                </ListView.View>
                            </ListView>
                            <Button x:Name="ButtonNameAndSetMappingNext" Content="Next" HorizontalAlignment="Center" Margin="410,350,0,0" VerticalAlignment="Top" Width="90" Height="20" Click="ButtonNameAndSetMappingNext_Click"/>
                        </Grid>
                        <Grid x:Name="GridImportMultipleUuidsSelection" Visibility="Collapsed">
                            <Label Content="Multiple versions of a card found!" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,140,0,0" FontSize="24" Style="{StaticResource BelerenBold}"/>
                            <TextBlock HorizontalAlignment="Center" Margin="0,184,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Height="140" FontSize="16">
                                <Run Text="The Good News: We found some of your imported cards in the card database. The Bad News: We found more than one version of the cards matching by card name + set name and/or set code."/>
                                <LineBreak/><LineBreak/>
                                <Run Text="So The Import Wizard needs a bit more help. Choose in the right column the specific version of the card(s) you are trying to import. Check the image reference on the right for help."/><LineBreak/>
                                <Run Text="You must select a version for all cards for the import to continue."/>
                            </TextBlock>
                            <DataGrid x:Name="MultipleUuidsDataGrid"
                                      AutoGenerateColumns="False" 
                                      CanUserAddRows="False"
                                      HorizontalAlignment="Center" Margin="0,380,0,0" Width="500">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Name of card to import" Binding="{Binding Name}" Width="*"/>
                                    <DataGridTemplateColumn Header="Select a version" Width="*">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <ComboBox ItemsSource="{Binding VersionedUuids}" 
                                                          DisplayMemberPath="DisplayText"
                                                          SelectedValuePath="Uuid"
                                                          SelectedValue="{Binding SelectedUuid, Mode=TwoWay}"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                            <Button x:Name="ButtonMultipleUuidsNext" Content="Next" HorizontalAlignment="Center" Margin="410,350,0,0" VerticalAlignment="Top" Width="90" Height="20" Click="ButtonMultipleUuidsNext_Click"/>
                        </Grid>
                        <Grid x:Name="GridImportAdditionalFieldsMapping" Visibility="Collapsed">
                            <Label Content="Mapping additional fields" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,140,0,0" FontSize="24" Style="{StaticResource BelerenBold}"/>
                            <TextBlock HorizontalAlignment="Center" Margin="0,184,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Height="140" FontSize="16">
                                <Run Text="Cards will be imported with the following default values if mapping is blank:"/>
                                <LineBreak/><LineBreak/>
                                <Run Text=" &#x2022; Condition: &quot;Near Mint&quot;"/><LineBreak/>
                                <Run Text=" &#x2022; Card Finish: &quot;nonfoil&quot;"/><LineBreak/>
                                <Run Text=" &#x2022; Cards Owned: &quot;1&quot;"/><LineBreak/>
                                <Run Text=" &#x2022; Cards For Trade/Selling: &quot;0&quot;"/><LineBreak/>
                                <Run Text=" &#x2022; Language: &quot;English&quot;"/><LineBreak/>
                            </TextBlock>
                            <ListView x:Name="AddionalFieldsMappingListView" Margin="0,380,0,0" Width="500" Height="180" HorizontalAlignment="Center" VerticalAlignment="Top">
                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn Header="Field to map" Width="150">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding CardSetField}" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn Header="Import file header" Width="200">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <ComboBox ItemsSource="{Binding CsvHeaders}" SelectedItem="{Binding CsvHeader}" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn Header="Reset mapping" Width="100">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Content="Clear Mapping" Width="85" Click="ClearMappingButton_Click"/>
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                    </GridView>
                                </ListView.View>
                            </ListView>
                            <Button x:Name="ButtonAdditionalFieldsNext" Content="Next" HorizontalAlignment="Center" Margin="410,350,0,0" VerticalAlignment="Top" Width="90" Height="20" Click="ButtonAdditionalFieldsNext_Click"/>
                        </Grid>
                        <Grid x:Name="GridImportCardConditionsMapping" Visibility="Collapsed">
                            <Label Content="Card conditions" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,140,0,0" FontSize="24" Style="{StaticResource BelerenBold}"/>
                            <TextBlock HorizontalAlignment="Center" Margin="0,184,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Height="140" FontSize="16">
                                <Run Text="Left column are the conditions found in the import file. Please map them to available conditions in the database (latter based on MCM conditions)."/>
                                <LineBreak/><LineBreak/>
                                <Run Text="Blank Conditions will be imported as &quot;Near Mint&quot;"/>
                            </TextBlock>
                            <ListView x:Name="ConditionsMappingListView" Margin="0,380,0,0" Width="500" HorizontalAlignment="Center" VerticalAlignment="Top">
                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn Header="Conditions found in import file" Width="180">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding CsvValue}" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn Header="Available conditions in database" Width="200">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <ComboBox ItemsSource="{Binding CardSetValue}" SelectedItem="{Binding SelectedCardSetValue}" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn Header="Reset mapping" Width="100">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Content="Clear Mapping" Width="85" Click="ClearMappingButton_Click" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                    </GridView>
                                </ListView.View>
                            </ListView>
                            <Button x:Name="ButtonConditionMappingNext" Content="Next" HorizontalAlignment="Center" Margin="410,350,0,0" VerticalAlignment="Top" Width="90" Height="20" Click="ButtonConditionMappingNext_Click"/>
                        </Grid>
                        <Grid x:Name="GridImportFinishesMapping" Visibility="Collapsed">
                            <Label Content="Card finishes" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,140,0,0" FontSize="24" Style="{StaticResource BelerenBold}"/>
                            <TextBlock HorizontalAlignment="Center" Margin="0,184,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Height="140" FontSize="16">
                                <Run Text="Left column are the finishes found in the import file. Please map them to available finishes in the database."/>
                                <LineBreak/><LineBreak/>
                                <Run Text="Blank Conditions will be imported as &quot;nonfoil&quot;"/>
                            </TextBlock>
                            <ListView x:Name="FinishesMappingListView" Margin="0,380,0,0" Width="500" HorizontalAlignment="Center" VerticalAlignment="Top">
                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn Header="Finishes found in import file" Width="180">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding CsvValue}" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn Header="Available conditions in database" Width="200">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <ComboBox ItemsSource="{Binding CardSetValue}" SelectedItem="{Binding SelectedCardSetValue}" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn Header="Reset mapping" Width="Auto">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Content="Clear Mapping" Width="85" Click="ClearMappingButton_Click"/>
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                    </GridView>
                                </ListView.View>
                            </ListView>
                            <Button x:Name="ButtonFinishesMappingNext" Content="Next" HorizontalAlignment="Center" Margin="410,350,0,0" VerticalAlignment="Top"  Width="90" Height="20" Click="ButtonFinishesMappingNext_Click"/>
                        </Grid>
                        <Grid x:Name="GridImportLanguageMapping" Visibility="Collapsed">
                            <Label Content="Card languages" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,140,0,0" FontSize="24" Style="{StaticResource BelerenBold}"/>
                            <TextBlock HorizontalAlignment="Center" Margin="0,184,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Height="140" FontSize="16">
                                <Run Text="Left column are the languages found in the import file. Please map them to available languages in the database."/>
                                <LineBreak/><LineBreak/>
                                <Run Text="Blank Languages will be imported as &quot;English&quot;"/>
                            </TextBlock>
                            <ListView x:Name="LanguageMappingListView" Margin="0,380,0,0" Width="500" HorizontalAlignment="Center" VerticalAlignment="Top">
                                <ListView.View>
                                    <GridView>
                                        <GridViewColumn Header="Languages found in import file" Width="180">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding CsvValue}" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn Header="Available languages in database" Width="200">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <ComboBox ItemsSource="{Binding CardSetValue}" SelectedItem="{Binding SelectedCardSetValue}" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn Header="Reset mapping" Width="100">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Content="Clear Mapping" Width="85" Click="ClearMappingButton_Click"/>
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                    </GridView>
                                </ListView.View>
                            </ListView>
                            <Button x:Name="ButtonLanguageMappingNext" Content="Next" HorizontalAlignment="Center" Margin="410,350,0,0" VerticalAlignment="Top"  Width="90" Height="20" Click="ButtonLanguageMappingNext_Click"/>
                        </Grid>
                        <Grid x:Name="GridImportConfirm" Visibility="Collapsed">
                            <Label Content="Summary and confirmation" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,140,0,0" FontSize="24" Style="{StaticResource BelerenBold}"/>
                            <Button x:Name="ButtonImportConfirm" Content="Looks good! Proceed with import..." HorizontalAlignment="Center" Margin="0,180,0,0" VerticalAlignment="Top" Width="200" Height="20" Click="ButtonImportConfirm_Click"/>
                            <Grid x:Name="GridImportSummary" Margin="0,210,0,0" Width="500">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="30"/>
                                    <RowDefinition Height="30"/>
                                    <RowDefinition Height="30"/>
                                    <RowDefinition Height="30"/>
                                    <RowDefinition Height="30"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <Grid Grid.Row="0" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center">
                                    <Label Content="Number of individual cards that will be added to collection:" HorizontalAlignment="Left" VerticalAlignment="Center" Height="25"  Style="{StaticResource BelerenBold}"/>
                                </Grid>
                                <Grid Grid.Row="0" Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Center">
                                    <Label x:Name="LabelIndividualCards" HorizontalAlignment="Left" VerticalAlignment="Center" Height="25" />
                                </Grid>
                                <Grid Grid.Row="1" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center">
                                    <Label Content="Total count of cards that will be added to collection:" HorizontalAlignment="Left" VerticalAlignment="Center" Height="25"  Style="{StaticResource BelerenBold}"/>
                                </Grid>
                                <Grid Grid.Row="1" Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Center">
                                    <Label x:Name="LabelTotalSumOfCards" HorizontalAlignment="Left" VerticalAlignment="Center" Height="25" />
                                </Grid>
                                <Grid Grid.Row="2" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center">
                                    <Label Content="Number of individual cards not possible to import:" HorizontalAlignment="Left" VerticalAlignment="Center" Height="25"  Style="{StaticResource BelerenBold}"/>
                                </Grid>
                                <Grid Grid.Row="2" Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Center">
                                    <Label x:Name="LabelSumOfUnAbleToImport" HorizontalAlignment="Left" VerticalAlignment="Center" Height="25" />
                                </Grid>
                                <Grid Grid.Row="3" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center">
                                    <Label x:Name="LabelUnableToImportCardsHeading" Content="Unable to import the following cards:" HorizontalAlignment="Left" VerticalAlignment="Center" FontSize="16" Style="{StaticResource BelerenBold}" Visibility="Collapsed"/>
                                </Grid>
                                <Grid Grid.Row="4" Grid.ColumnSpan="2" HorizontalAlignment="Left" VerticalAlignment="Center">
                                    <Button x:Name="SaveListOfUnimportedItems" Content="Save list to text file" HorizontalAlignment="Right" VerticalAlignment="Center" Width="120" Height="20" Visibility="Collapsed" Click="SaveListOfUnimportedItems_Click"/>
                                </Grid>
                                <Grid Grid.Row="5" Grid.ColumnSpan="2" HorizontalAlignment="Left" VerticalAlignment="Top">
                                    <StackPanel x:Name="ImportSummaryContainer" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,0,0,0" Width="500">
                                        <TextBlock x:Name="ImportSummaryTextBlock" TextWrapping="Wrap" Height="Auto"/>
                                    </StackPanel>
                                </Grid>
                            </Grid>
                        </Grid>
                        <Grid x:Name="GridImportSuccess" Visibility="Collapsed">
                            <Label Content="Import complete!" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,140,0,0" FontSize="24" Style="{StaticResource BelerenBold}"/>
                            <TextBlock HorizontalAlignment="Center" Margin="0,184,0,0" TextWrapping="Wrap" VerticalAlignment="Top" FontSize="16" Height="18.5">
                                <Run Text="The cards have now been added to your collection."/>
                            </TextBlock>
                            <Image Margin="0,230,0,0" Source="Resources/Images/IxidorsWill.jpg" HorizontalAlignment="Center" VerticalAlignment="Top" Height="274" Width="344" Opacity="0.7"/>
                            <Button x:Name="ButtonEndImportWizard" Content="Awesome! Take me to my collection..." HorizontalAlignment="Center" Margin="0,540,0,0" VerticalAlignment="Top" Width="220" Height="20" Click="ButtonEndImportWizard_Click"/>
                        </Grid>
                    </Grid>
                </Grid>
                <!-- Status window -->
                <local:StatusOverlay DataContext="{Binding DataContext.StatusOverlay, RelativeSource={RelativeSource AncestorType=Window}}" d:IsHidden="True"/>
            </Grid>
            <Grid x:Name="GridCardImages" Grid.Row="1" Grid.Column="2" HorizontalAlignment="Right" Width="250" Margin="0,0,0,0" Visibility="{Binding CardViewSectionVisibility}">
                <Label x:Name="ImagePromoLabel" Content="{Binding CardImageVM.ImagePromoType}" HorizontalAlignment="Left" VerticalAlignment="Top" Height="30" Width="250" Style="{StaticResource BelerenBold}"/>
                <Label x:Name="ImageSetLabel" Content="{Binding CardImageVM.ImageSet}" HorizontalAlignment="Left" VerticalAlignment="Top" Height="30" Width="250" Margin="0,35,0,0" Style="{StaticResource BelerenBold}"/>
                <Image x:Name="CardImage"
                       Source="{Binding CardImageVM.FrontImageSource}"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Top"
                       Margin="0,80,10,0"/>
                <Image x:Name="CardImageSecondary"
                       Source="{Binding CardImageVM.BackImageSource}"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Top" Margin="0,461,10,0"/>
            </Grid>
        </Grid>
    </Grid>
</Window>

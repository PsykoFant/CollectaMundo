<UserControl x:Class="CollectaMundo.Views.TopMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:presentation="clr-namespace:CollectaMundo.Presentation" 
             xmlns:vm="clr-namespace:CollectaMundo.ViewModels.Shell" 
             d:DataContext="{d:DesignInstance Type=vm:TopMenuViewModel}"
             mc:Ignorable="d">

    <Grid IsEnabled="{Binding IsTopMenuEnabled}" VerticalAlignment="Top" HorizontalAlignment="Center" Height="40">

        <Button Content="All Cards" Command="{Binding ShowAllCardsPageCommand}" Margin="0,5,510,5">
            <Button.Style>
                <Style TargetType="Button" BasedOn="{StaticResource TopMenuButtonStyle}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsAllCardsPageActive}" Value="True">
                            <Setter Property="Background" Value="#5cb9ca"/>
                            <Setter Property="BorderBrush" Value="#2f6f7a"/>
                            <Setter Property="Foreground" Value="#2f6f7a"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>

        <Button Content="My Collection" Command="{Binding ShowMyCollectionPageCommand}" Margin="0,5,170,5">
            <Button.Style>
                <Style TargetType="Button" BasedOn="{StaticResource TopMenuButtonStyle}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsMyCollectionPageActive}" Value="True">
                            <Setter Property="Background" Value="#5cb9ca"/>
                            <Setter Property="BorderBrush" Value="#2f6f7a"/>
                            <Setter Property="Foreground" Value="#2f6f7a"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>

        <Button Content="Decks" Margin="170,5,0,5">
            <Button.Style>
                <Style TargetType="Button" BasedOn="{StaticResource TopMenuButtonStyle}">
                    <Style.Triggers>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>

        <Button Content="Utilities" Command="{Binding ShowUtilitiesPageCommand}" Margin="510,5,0,5">
            <Button.Style>
                <Style TargetType="Button" BasedOn="{StaticResource TopMenuButtonStyle}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsUtilitiesPageActive}" Value="True">
                            <Setter Property="Background" Value="#5cb9ca"/>
                            <Setter Property="BorderBrush" Value="#2f6f7a"/>
                            <Setter Property="Foreground" Value="#2f6f7a"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>        
    </Grid>
</UserControl>

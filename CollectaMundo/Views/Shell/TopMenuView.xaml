<UserControl x:Class="CollectaMundo.Views.TopMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:presentation="clr-namespace:CollectaMundo.Presentation" 
             xmlns:viewmodels="clr-namespace:CollectaMundo.ViewModels" 
             d:DataContext="{d:DesignInstance Type=viewmodels:MainWindowViewModel}"
             mc:Ignorable="d">

    <!-- NOTE:
         - DataContext is inherited from MainWindow (Binding Main is on AppWindow grid).
         - StaticResource lookups (TopMenuButtonStyle, PageToBrushConverter) will still work
           as long as they live in App.xaml / merged dictionaries / window resources. -->

    <Grid x:Name="GridTopMenu"
          VerticalAlignment="Top"
          HorizontalAlignment="Center"
          Height="40"
          IsEnabled="{Binding IsTopMenuEnabled}">

        <Button Command="{Binding ShowSearchAndFilterCommand}"
                Background="{Binding CurrentPage,Converter={StaticResource PageToBrushConverter},ConverterParameter={x:Static presentation:Page.SearchAndFilter}}"
                Content="All Cards"
                HorizontalAlignment="Center"
                Margin="0,5,510,0"
                VerticalAlignment="Top"
                Width="160"
                Height="30"
                Style="{StaticResource TopMenuButtonStyle}">
            <Button.Effect>
                <DropShadowEffect ShadowDepth="5" BlurRadius="10" Opacity="0.6"/>
            </Button.Effect>
        </Button>

        <Button Command="{Binding ShowMyCollectionCommand}"
                Background="{Binding CurrentPage,Converter={StaticResource PageToBrushConverter},ConverterParameter={x:Static presentation:Page.MyCollection}}"
                Content="My Collection"
                HorizontalAlignment="Center"
                Margin="0,5,170,0"
                VerticalAlignment="Top"
                Width="160"
                Height="30"
                Style="{StaticResource TopMenuButtonStyle}">
            <Button.Effect>
                <DropShadowEffect ShadowDepth="5" BlurRadius="10" Opacity="0.6"/>
            </Button.Effect>
        </Button>

        <Button Command="{Binding ShowDecksCommand}"
                Background="{Binding CurrentPage,Converter={StaticResource PageToBrushConverter},ConverterParameter={x:Static presentation:Page.Decks}}"
                Content="Decks"
                HorizontalAlignment="Center"
                Margin="170,5,0,0"
                VerticalAlignment="Top"
                Width="160"
                Height="30"
                Style="{StaticResource TopMenuButtonStyle}">
            <Button.Effect>
                <DropShadowEffect ShadowDepth="5" BlurRadius="10" Opacity="0.6"/>
            </Button.Effect>
        </Button>

        <Button Command="{Binding ShowUtilitiesCommand}"
                Background="{Binding CurrentPage,Converter={StaticResource PageToBrushConverter},ConverterParameter={x:Static presentation:Page.Utilities}}"
                Content="Utilities"
                HorizontalAlignment="Center"
                Margin="510,5,0,0"
                VerticalAlignment="Top"
                Width="160"
                Height="30"
                Style="{StaticResource TopMenuButtonStyle}">
            <Button.Effect>
                <DropShadowEffect ShadowDepth="5" BlurRadius="10" Opacity="0.6"/>
            </Button.Effect>
        </Button>

    </Grid>
</UserControl>

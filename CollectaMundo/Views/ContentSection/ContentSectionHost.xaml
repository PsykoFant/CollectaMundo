<UserControl x:Class="CollectaMundo.Views.ContentSection.ContentSectionHost"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"             
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:CollectaMundo.ViewModels"
             xmlns:vmPages="clr-namespace:CollectaMundo.ViewModels.Pages"             
             xmlns:pageViews="clr-namespace:CollectaMundo.Views.ContentSection.Pages"             
             d:DataContext="{d:DesignInstance Type=viewmodels:MainWindowViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10,0,10,0">
        <ContentControl Content="{Binding CurrentPageViewModel}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <ContentControl.Resources>
                <DataTemplate DataType="{x:Type vmPages:PagesSearchAndFilterViewModel}"><pageViews:PagesSearchAndFilterView /></DataTemplate>
                <DataTemplate DataType="{x:Type vmPages:PagesMyCollectionViewModel}"><pageViews:PagesMyCollectionView /></DataTemplate>
                <DataTemplate DataType="{x:Type vmPages:PagesDecksHostViewModel}"><pageViews:PagesDecksHost /></DataTemplate>
                <DataTemplate DataType="{x:Type vmPages:PagesUtilitiesHostViewModel}"><pageViews:PagesUtilitiesHost /></DataTemplate>
            </ContentControl.Resources>
        </ContentControl>
    </Grid>
</UserControl>

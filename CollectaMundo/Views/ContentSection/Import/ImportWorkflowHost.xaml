<UserControl x:Class="CollectaMundo.Views.ContentSection.Import.ImportWorkflowHost"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:stepViews="clr-namespace:CollectaMundo.Views.ContentSection.Import"
             xmlns:vm="clr-namespace:CollectaMundo.ViewModels.Import"
             xmlns:vmsteps="clr-namespace:CollectaMundo.ViewModels.Import.ImportSteps"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance vm:ImportViewModel}"
             Style="{StaticResource {x:Type UserControl}}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Headers -->
                <RowDefinition Height="Auto"/> <!-- Fail image -->
                <RowDefinition Height="Auto"/> <!-- Step content -->
                <RowDefinition Height="Auto"/> <!-- Progress -->
                <RowDefinition Height="Auto"/> <!-- Buttons -->
            </Grid.RowDefinitions>

        <!-- Headers -->
        <StackPanel Grid.Row="0"
                HorizontalAlignment="Center"
                Margin="0,10,0,10">
                <Label Content="{Binding ProgressStep}" Style="{StaticResource MediumHeaderLabelStyle}" />
                <Label Content="{Binding ProgressHeadline}" Style="{StaticResource BigHeaderLabelStyle}" />
            </StackPanel>

        <!-- Fail image -->
        <Image Grid.Row="1"
           Source="/Resources/Images/counterspell_fail.png"
           Visibility="{Binding ImportFailVisibility}"
           Stretch="None"
           HorizontalAlignment="Center"
           Margin="0,10,0,10"/>

        <!-- Success image -->
        <Image Grid.Row="1"
           Source="/Resources/Images/IxidorsWill.jpg"
           Visibility="{Binding ImportSuccessVisibility}"
           Stretch="None"
           HorizontalAlignment="Center"
           Margin="0,10,0,10"/>

        <!-- Step content -->
        <ScrollViewer Grid.Row="2" MaxHeight="600" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <ContentControl Width="550" Content="{Binding CurrentStepViewModel}">
            <ContentControl.Resources>
                <DataTemplate DataType="{x:Type vmsteps:ImportStep01_StartViewModel}"><stepViews:ImportStep01_StartView /></DataTemplate>
                <DataTemplate DataType="{x:Type vmsteps:ImportStep02_IdMappingViewModel}"><stepViews:ImportStep02_IdMappingView /></DataTemplate>
                <DataTemplate DataType="{x:Type vmsteps:ImportStep03_NameSetMappingViewModel}"><stepViews:ImportStep03_NameSetMappingView /></DataTemplate>
                <DataTemplate DataType="{x:Type vmsteps:ImportStep04_MultipleUuidsViewModel}"><stepViews:ImportStep04_MultipleUuuidsView /></DataTemplate>
                <DataTemplate DataType="{x:Type vmsteps:ImportStep05_AdditionalFieldsMappingViewModel}"><stepViews:ImportStep05_AdditionalFieldsMappingView /></DataTemplate>
                <DataTemplate DataType="{x:Type vmsteps:ImportStep06_ConditionsMappingViewModel}"><stepViews:ImportStep06_ConditionsMappingView /></DataTemplate>
                <DataTemplate DataType="{x:Type vmsteps:ImportStep07_FinishMappingViewModel}"><stepViews:ImportStep07_FinishesMappingView /></DataTemplate>
                <DataTemplate DataType="{x:Type vmsteps:ImportStep08_LanguageMappingViewModel}"><stepViews:ImportStep08_LanguagesMappingView /></DataTemplate>
                <DataTemplate DataType="{x:Type vmsteps:ImportStep10_SummaryViewModel}"><stepViews:ImportStep10_SummaryView /></DataTemplate>
                <DataTemplate DataType="{x:Type vmsteps:ImportStep11_FinishViewModel}"><stepViews:ImportStep11_FinishView /></DataTemplate>
            </ContentControl.Resources>
        </ContentControl>
        </ScrollViewer>

        <!-- Progress bar + message -->
        <StackPanel Grid.Row="3" HorizontalAlignment="Center">
            <ProgressBar Value="{Binding ProgressValue}" Style="{StaticResource CollectaMundoProgressBarStyle}" Visibility="{Binding ProgressVisibility}"/>
            <Label Content="{Binding ProgressDetailMessage}" HorizontalAlignment="Center"/>
        </StackPanel>
            
        <!-- Buttons --> 
        <Grid Grid.Row="4" Width="550">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Cancel -->
            <Button Grid.Column="0"
                    Content="  Cancel import &#x274C;  " 
                    Command="{Binding CancelCommand}" 
                    Visibility="{Binding CancelVisibility}" 
                    HorizontalAlignment="Left"/>

            <!-- Primary Action -->
            <Button Grid.Column="1"
                    Content="{Binding CurrentStepViewModel.PrimaryActionButtonText}"
                    Command="{Binding PrimaryActionCommand}"
                    IsEnabled="{Binding IsPrimaryActionButtonEnabled}"
                    Visibility="{Binding CurrentStepViewModel.PrimaryActionVisibility}"
                    HorizontalAlignment="Center"/>

            <!-- Secondary Action -->
            <Button Grid.Column="2"
                    Content="{Binding CurrentStepViewModel.SecondaryActionButtonText}"
                    Command="{Binding SecondaryActionCommand}"
                    IsEnabled="{Binding IsSecondaryActionButtonEnabled}"
                    Visibility="{Binding CurrentStepViewModel.SecondaryActionVisibility}"
                    HorizontalAlignment="Right"/>
        </Grid>
    </Grid>
</UserControl>
